# Project Summary

项目提供面向企业知识运营、智能运维与知识治理的统一 AIOps 平台。平台接收外部告警，通过事件标准化、消息队列、缓存、向量检索和多层业务重排生成根因诊断，并在 Web 控制台中管理知识库、案例、规则、Agent 流程、审批和运维治理。

平台包含三类 Agent：

- **深度诊断 Agent**：结合告警、CMDB、日志、规则和知识库进行多轮根因分析。
- **值班 Agent**：聚合服务影响面，生成值班报告和 ChatOps 信息。
- **知识治理 Agent**：聚类告警、生成案例草稿、执行相似案例治理并提交审批。

RAG 流水线基于 LangChain 构建，使用 Milvus 召回候选案例，并通过 L1 规则过滤、L2 多特征融合、L3 Cross-Encoder 和可选的 L4 LLM Listwise 重排输出参考案例。各层支持超时、熔断和逐级降级。

知识治理已补齐基础 RAG 闭环：知识案例发布、回滚、合并和归档可同步至 RAG 索引，支持 upsert、delete、verify、按 `case_id` 查询及反馈统计保留；控制台新增案例归档和生命周期巡检能力，并提供敏感日志脱敏、LIKE 通配符转义和 Agent 失败会话落库兜底。

Agent 工作台未绑定角色的账号默认解析为 `sre`，可显示诊断、值班和知识治理操作；后端授权校验保持不变，`default_role` 仍禁止设置为 `sys_admin`。

# Project Module Description

- **RAG 告警流水线**：接收 Webhook，标准化告警、生成事件指纹并写入 Kafka/Redis，通过 Milvus 和 LLM 完成诊断。
- **知识索引同步**：
  - 提供知识案例 upsert、delete、verify 接口。
  - 支持发布、回滚、合并和归档后的索引同步。
  - 支持按 `case_id` 查询，并保留反馈、命中和召回统计。
  - 同步失败目前由调用方记录和降级处理，重试及补偿队列属于后续迭代。
- **LangChain 检索层**：
  - 将 Milvus 结果转换为 LangChain `Document`。
  - 使用 `BaseRetriever` 统一检索接口。
  - 使用 LCEL 编排提示词、LLM 和结构化输出。
- **四层业务重排**：
  - L1：按有效期、黑名单、反馈和废弃方案硬过滤。
  - L2：融合语义、拓扑、时间衰减、反馈、模板、新鲜度和命中率。
  - L3：使用 BGE Cross-Encoder 精排。
  - L4：使用 LLM 进行 Listwise 排序，失败时回退至 L3/L2。
- **深度诊断 Agent**：最多执行 6 轮 ReAct 推理，支持工具取证、非法 JSON 修正、证据链校验和单轮诊断回退。
- **值班 Agent**：按 CMDB 服务、负责人和严重级别聚合影响面，支持 AI 报告和确定性降级报告。
- **知识治理 Agent**：支持告警聚类、案例草稿、幂等保护、相似案例扫描、审批发布、会话恢复、归档和生命周期巡检。
- **Agent 工作台**：
  - 诊断和值班手动触发按钮需要 `can_diagnose`。
  - 知识治理手动触发按钮需要 `can_edit_kb`。
  - 未绑定角色的账号默认使用 `sre`，可访问三个 Agent 操作。
  - `operator` 按角色层级只能触发诊断和值班报告。
  - `sys_admin` 不可作为默认角色。
- **权限与认证**：支持 OIDC、Token Exchange、演示登录、Bearer JWT 和 RBAC，后端执行最终授权。
- **知识库与案例**：管理知识条目、案例、版本、日志、拓扑、变更记录和 RAG 索引状态。
- **审批中心**：处理案例变更、规则升级及新建、更新、合并和归档审批。
- **规则管理**：支持规则校验、发布、回滚、晋升、废弃和未知告警治理。
- **生命周期治理**：支持归档接口和定期巡检，按有效期或长期未更新条件发现候选案例。
- **性能与监控**：监控 RAG 阶段、重排层、Agent 延迟、降级次数和知识同步统计。
- **反馈闭环**：记录点赞、点踩、命中和召回统计，用于案例评分及后续训练；反馈双写统一和自动质量学习仍待完善。

# Directory Tree

```text
README.md                              # 项目总览、模块边界与安全说明
INSTALL.md                             # 安装与部署说明
LICENSE                                # Apache-2.0 许可证
CONTRIBUTING.md                        # 贡献与 PR 规范
CODE_OF_CONDUCT.md                     # 社区行为准则
SECURITY.md                            # 漏洞报告与安全要求
install.sh                             # 一键安装脚本
.github/workflows/ci.yml               # 持续集成配置

docs/
├── DEMO_ACCOUNTS.md                   # 演示账号说明
├── CONSOLE_USER_GUIDE.md              # 控制台使用手册
├── CONSOLE_FAQ.md                     # 常见问题
├── OPERATIONS_DEPLOYMENT_GUIDE.md     # 部署方案
├── OPERATIONS_RUNBOOK.md              # 值班与故障处理手册
├── KB_GOVERNANCE_AGENT_DESIGN.md      # 知识治理 Agent 设计
├── DIAGNOSE_AGENT_DESIGN.md           # 深度诊断 Agent 设计
├── ONCALL_AGENT_DESIGN.md             # 值班 Agent 设计
└── OPEN_SOURCE_RELEASE_CHECKLIST.md   # 开源发布检查清单

aiops-rag-system/
├── README.md                          # RAG 与业务重排说明
├── requirements.txt                   # Python 与 AI 依赖
├── docker-compose.yml                 # RAG 依赖服务编排
├── .env.example                       # RAG 配置模板
├── src/
│   ├── main.py                        # FastAPI 应用、探针与路由注册
│   ├── config.py                      # 运行与重排配置
│   ├── kafka_producer.py              # Kafka 消息生产
│   ├── models/                        # 案例和响应模型
│   ├── api/
│   │   ├── diagnostic.py              # 诊断接口
│   │   └── kb_sync.py                 # 知识索引同步接口
│   ├── rag_pipeline/
│   │   ├── milvus_client.py           # Milvus 检索、按案例查询与索引操作
│   │   └── pipeline.py                # RAG 流水线编排
│   ├── storage/redis_client.py        # Redis 访问
│   └── utils/metrics.py               # Prometheus 指标
└── tests/                             # RAG、API、同步和性能测试

app/
├── backend/
│   ├── README.md                      # 后端说明
│   ├── .env.example                   # 后端配置模板
│   ├── scripts/                       # 演示数据和数据库修复脚本
│   ├── alembic/                       # 数据库迁移
│   ├── routers/
│   │   ├── console.py                 # 控制台业务、归档与生命周期路由
│   │   └── console_agent.py           # Agent、会话和报告路由
│   └── services/
│       ├── console_common.py          # 公共配置、权限、角色默认值与脱敏工具
│       ├── console_agent.py           # Agent 执行、失败会话与结果持久化
│       ├── console_kb.py              # 知识库、案例、审批、生命周期和索引钩子
│       └── rag_sync.py                # 控制台到 RAG 的同步客户端
└── frontend/
    ├── README.md                      # 前端说明
    ├── .env.example                   # 前端配置模板
    ├── pnpm-lock.yaml                 # 依赖锁定文件
    └── src/
        ├── lib/console-api.ts         # API 类型与调用封装
        ├── components/                # 通用组件与布局
        └── pages/                     # 控制台和帮助页面
```

`.atoms/`、`.mgx/`、测试缓存、安装包、日志、临时脚本和构建产物不属于源码发布内容。`app/backend/skills_docs/` 等内部模板文档不应纳入开源发布。

# File Description Inventory

| 文件 | 说明 |
|---|---|
| `aiops-rag-system/src/main.py` | 注册诊断和知识索引同步接口，提供 RAG 服务入口。 |
| `aiops-rag-system/src/api/kb_sync.py` | 提供知识案例 upsert、delete、verify 同步 API。 |
| `aiops-rag-system/src/rag_pipeline/milvus_client.py` | 管理 Milvus 案例索引、按 `case_id` 查询和统计字段。 |
| `aiops-rag-system/src/rag_pipeline/pipeline.py` | 执行检索、重排及知识结果统计处理。 |
| `aiops-rag-system/tests/test_api.py` | 覆盖 RAG API 和知识同步闭环测试。 |
| `app/backend/routers/console.py` | 提供控制台业务、案例归档和生命周期巡检接口。 |
| `app/backend/services/console_common.py` | 定义控制台配置、角色权限、默认角色、输入处理和敏感信息脱敏。 |
| `app/backend/services/console_agent.py` | 实现 Agent 执行、会话恢复、失败状态落库、报告生成和结果持久化。 |
| `app/backend/services/console_kb.py` | 实现知识库、案例治理、审批、发布、回滚、合并、归档和 RAG 同步钩子。 |
| `app/backend/services/rag_sync.py` | 封装控制台调用 RAG 知识同步接口的客户端。 |
| `app/backend/scripts/seed_console_demo.py` | 写入演示数据并重置业务表序列。 |
| `app/backend/scripts/fix_sequences.py` | 修复 PostgreSQL 自增序列。 |
| `aiops-rag-system/src/rag_pipeline/retriever.py` | 将 Milvus 检索封装为 LangChain `BaseRetriever`。 |
| `aiops-rag-system/src/rag_pipeline/rerank_pipeline.py` | 编排 L1-L4 重排和降级流程。 |
| `aiops-rag-system/src/rag_pipeline/reranker_l1.py` | 执行规则硬过滤。 |
| `aiops-rag-system/src/rag_pipeline/reranker_l2.py` | 计算多特征业务评分。 |
| `aiops-rag-system/src/rag_pipeline/reranker_l3.py` | 执行 Cross-Encoder 精排。 |
| `aiops-rag-system/src/rag_pipeline/reranker_l4.py` | 执行 LLM Listwise 重排。 |
| `aiops-rag-system/src/utils/metrics.py` | 定义延迟、降级、结果数量和同步统计指标。 |
| `app/frontend/src/lib/console-api.ts` | 定义控制台 API、权限和 Agent 调用类型。 |
| `app/frontend/src/pages/console/AgentsPage.tsx` | 展示 Agent 任务、手动触发入口、会话和历史报告。 |
| `app/frontend/src/pages/console/EventsPage.tsx` | 管理告警详情、诊断和反馈。 |
| `app/frontend/src/pages/console/KbPage.tsx` | 管理案例、发布、回滚、归档和相似案例合并。 |
| `app/frontend/src/pages/console/ApprovalsPage.tsx` | 展示审批证据、字段 Diff 和决策操作。 |
| `app/frontend/src/pages/console/RulesPage.tsx` | 管理规则版本和未知告警队列。 |
| `app/frontend/src/pages/console/OpsPage.tsx` | 提供审计、系统配置和治理运维页面。 |

# Technology Stack

- **后端**：Python、FastAPI、Uvicorn
- **AI 编排**：LangChain、LCEL、ReAct、结构化输出
- **向量检索**：Milvus、BGE-M3 Embedding
- **业务重排**：规则过滤、多特征融合、BGE Cross-Encoder、LLM Listwise
- **消息与缓存**：Kafka、Redis
- **数据存储**：PostgreSQL、Alembic
- **可观测性**：Prometheus Histogram、Agent 降级、报告和知识同步指标
- **认证授权**：OIDC、JWT、RBAC
- **前端与交付**：TypeScript、Vite、pnpm、Docker Compose、Nginx
- **质量保障**：pytest、compileall、ESLint、GitHub Actions
- **开源治理**：Apache-2.0、贡献指南、安全策略和发布检查清单

# Usage

## 安装依赖

在源码目录执行：

```bash
bash install.sh
```

按模块跳过安装：

```bash
bash install.sh --skip-rag
bash install.sh --skip-backend
bash install.sh --skip-frontend
```

单独安装依赖：

```bash
cd aiops-rag-system
pip install -r requirements.txt

cd ../app/frontend
pnpm install
```

## 配置

根据模板创建本地配置：

- `aiops-rag-system/.env.example`
- `app/backend/.env.example`
- `app/frontend/.env.example`

除数据库、Milvus、Kafka、Redis、Embedding、LLM、CMDB 和重排参数外，还需配置控制台访问 RAG 服务的地址及同步超时参数。

生产环境必须设置强随机 `JWT_SECRET_KEY` 和 `MASK_KEY`，并关闭演示登录：

```bash
ENABLE_DEMO_LOGIN=false
```

原始日志和模型输入应经过敏感信息脱敏处理。

## 测试与检查

```bash
cd aiops-rag-system
pytest
python -m compileall src

cd ../app/backend
python -m compileall .

cd ../frontend
pnpm run lint
pnpm run build
```

RAG 测试覆盖诊断 API、知识同步 API、Milvus 查询和同步统计；控制台回归需额外验证后端导入、路由注册、权限解析和生命周期巡检。

## 初始化和数据修复

使用演示数据时执行种子脚本。脚本会同步业务表自增序列。若数据库曾被显式主键写入污染，可执行：

```bash
cd app/backend
python scripts/fix_sequences.py
```

执行前需确保数据库连接配置有效。

## 运行服务

运行 RAG 服务：

```bash
cd aiops-rag-system
uvicorn src.main:app
```

控制台后端按照 `app/backend/README.md` 准备数据库、依赖和环境变量后启动。前端开发模式使用：

```bash
cd app/frontend
pnpm run dev
```

## 知识治理闭环验证

1. 创建或更新案例并提交审批。
2. 审批发布后验证 RAG 侧 upsert 结果。
3. 执行案例回滚、合并和归档，验证对应索引操作。
4. 使用 verify 按 `case_id` 检查索引可见性和统计字段。
5. 执行生命周期巡检的 dry-run，确认到期或长期未更新候选。
6. 提交诊断反馈，验证反馈、命中和召回统计保留。
7. 模拟同步或 Agent 异常，确认降级行为和 `failed` 会话落库。
8. 验证敏感日志脱敏及 LIKE 查询条件转义。

## 权限验证

- 未绑定角色的账号默认解析为 `sre`，刷新页面后可看到三个 Agent 手动触发按钮。
- `operator` 只能使用诊断和值班报告功能。
- `sre`、更高等级角色可使用知识治理功能。
- 如需调整账号角色，在“审计与配置”中修改 `role_bindings_json`。
- 不应将 `default_role` 设置为 `sys_admin`。

## CI 验证

GitHub Actions 自动执行：

1. RAG pytest；
2. 后端 Python 静态编译检查；
3. 前端 ESLint 和生产构建。

当前已验证 RAG 全量测试通过，控制台后端可正常导入，归档与生命周期巡检路由注册正常。
