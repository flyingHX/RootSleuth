# Project Summary

项目是面向企业知识运营、智能运维和知识治理的统一 AIOps 平台。平台接收外部告警，经事件标准化、消息队列、缓存、向量检索、业务重排和 Agent 推理，输出根因诊断、值班报告、ChatOps 文本及知识治理结果，并通过 Web 控制台管理知识库、案例、规则、会话、审批和运维任务。

当前业务闭环包括：

- **告警诊断**：通过 ReAct Agent、多层检索重排和证据链校验完成根因分析。
- **值班报告**：按服务、负责人和严重级别聚合影响面，生成持久化报告和 ChatOps 文本。
- **知识库构建与同步**：支持案例 upsert、delete、verify、版本守卫、补偿同步、缓存失效和反馈回流。
- **知识治理**：支持告警聚类、案例草稿、审批发布、相似案例合并、归档、安全扫描和生命周期巡检。
- **知识去重合并**：仅当同错误类型、同服务的案例模板相似度大于等于 80% 时展示合并建议，并提供组内相似度供人工判断。
- **质量与可信度治理**：统一输出 Faithfulness、Context Coverage、Answer Relevance、幻觉率和 Trust Index。
- **安全与可靠性**：支持鉴权、租户隔离、内容安全、幂等、版本控制、补偿队列和审计。
- **独立 Agent LLM 配置**：深度诊断、知识治理和值班三个 Agent 可分别配置 Provider、Base URL、API Key、模型、温度和超时；未填写字段逐项继承全局默认配置。
- **动态配置生效**：配置保存后无需重启即可生效，并支持按 Agent 测试实际连接参数和继承来源。
- **生产启动防护**：生产环境启动脚本自动禁用 Uvicorn `--reload`，降低进程重载造成的源站空窗和网关 502 风险。

本轮知识库去重合并改造已完成，后端回归 24 项、前端 lint/build 均通过。

# Project Module Description

- **RAG 告警流水线**
  - 接收并标准化告警，生成事件指纹。
  - 通过 Kafka、Redis、Milvus 和 LLM 完成召回、重排和诊断。
  - 支持请求认证、租户/集群/环境过滤、证据链和降级处理。

- **知识搜索服务**
  - 提供带鉴权和数据隔离的知识搜索接口。
  - 使用向量召回、业务重排和 PostgreSQL 限量扫描回退。
  - 返回召回、排序、Embedding 和端到端耗时指标。

- **知识发布与同步**
  - 支持案例 upsert、delete、verify、反馈同步和版本守卫。
  - 失败任务进入补偿队列，支持退避、死信、人工重放和审计。
  - 使用并发领取锁避免任务重复执行。

- **语义缓存**
  - 使用 epoch 标识缓存版本。
  - 发布、删除和反馈变更时失效缓存，并通过 Redis Pub/Sub 广播多实例失效事件。

- **四层业务重排**
  - L1 执行有效期、黑名单、反馈和废弃状态过滤。
  - L2 融合语义、拓扑、时间衰减、反馈、新鲜度等业务特征。
  - L3 使用 Cross-Encoder 精排。
  - L4 使用 LLM Listwise 排序，失败时回退至 L3/L2。
  - 各级排序使用稳定 tie-breaker。

- **检索安全与内容治理**
  - 支持 API Key/JWT、租户/集群/环境隔离。
  - 对知识内容执行密钥、PII、危险命令和提示词注入检测。
  - 支持摘要脱敏、跨字段去重、误报放行和 `allow_override`。

- **深度诊断 Agent**
  - 支持多轮 ReAct、并行工具调用、格式纠错、证据校验和降级。
  - LLM 配置按 `diagnose` 作用域解析，字段为空时逐项继承全局配置。
  - 支持独立模型、温度和超时，以及 30–600 秒的诊断时间预算。
  - 持久化输入指纹、候选序列、规则版本、模型参数、工具轨迹、质量和稳定性快照。
  - 支持成功及降级路径重复运行一致性验证。

- **知识治理 Agent**
  - 支持告警聚类、案例草稿、审批发布、相似案例扫描、合并、归档和巡检。
  - 自动合并建议复用知识库去重扫描逻辑，仅处理模板相似度达到 80% 的同服务、同错误类型案例。
  - 使用 `kb_governance` 独立 LLM 配置，支持逐项继承全局默认值。
  - 持久化草稿质量、Trust Index、工具轨迹和审计结果。

- **值班 Agent**
  - 按 CMDB 服务、负责人和严重级别聚合影响面。
  - 使用 `oncall` 独立 LLM 配置，支持逐项继承全局默认值。
  - 生成 AI 报告、ChatOps 文本和确定性降级报告，并持久化质量结果。

- **知识库去重合并**
  - 扫描同错误类型、同服务的案例候选。
  - 使用 `KB_DEDUP_SIMILARITY_THRESHOLD=0.8` 作为相似度阈值。
  - 相似度大于等于 80% 才形成合并组并展示建议。
  - 不再以同集群作为低相似度案例的兜底成组条件。
  - 返回组内两两相似度和组内最低相似度，辅助人工确认合并合理性。
  - 前端展示候选案例与主案例的相似度徽标。

- **配置中心**
  - 提供全局 LLM 默认配置及三个 Agent 独立配置分组。
  - 每个 Agent 支持 Provider、Base URL、API Key、模型、温度和超时，共 15 个独立接入键。
  - API Key 使用 Fernet 加密存储，不明文回显。
  - 配置为空时继承对应全局字段；支持保存、动态下发和按 Agent 连通性测试。
  - 连通性测试返回实际 Provider、模型、超时及配置来源。

- **Agent 质量展示**
  - Agents、Ops 和 Dashboard 页面展示 Trust Index、质量明细、稳定性、模型参数和时间预算。
  - 配置中心展示全局默认及三个 Agent LLM 配置分组。
  - API Key 使用脱敏占位符，支持继承全局下拉选项和独立测试按钮。

- **权限与认证**
  - 控制台支持 OIDC、Token Exchange、演示登录、Bearer JWT 和 RBAC。
  - `/webhook` 保持免鉴权契约。
  - 未绑定角色的账号默认使用 `sre`，`operator` 只能触发诊断和值班报告。

# Directory Tree

```text
README.md                              # 项目总览、模块边界与安全说明
INSTALL.md                             # 安装与部署说明
LICENSE                                # Apache-2.0 许可证
CONTRIBUTING.md                        # 贡献与 PR 规范
SECURITY.md                            # 漏洞报告与安全要求
install.sh                             # 一键安装脚本
.github/workflows/ci.yml               # 持续集成配置

docs/
├── CONSOLE_USER_GUIDE.md              # 控制台、质量卡、LLM 配置和知识去重说明
├── CONSOLE_FAQ.md                     # 常见问题
├── OPERATIONS_DEPLOYMENT_GUIDE.md     # 部署、鉴权和配置中心说明
├── OPERATIONS_RUNBOOK.md              # 值班、质量告警和故障处理
├── KB_GOVERNANCE_AGENT_DESIGN.md      # 知识治理 Agent 设计、配置和去重规则
├── DIAGNOSE_AGENT_DESIGN.md           # 诊断 Agent 设计与配置
├── ONCALL_AGENT_DESIGN.md             # 值班 Agent 设计与配置
├── RAG_TRUST_EVALUATION.md            # RAG 可信度与质量评估
└── OPEN_SOURCE_RELEASE_CHECKLIST.md   # 开源发布清单

aiops-rag-system/
├── requirements.txt                   # Python 与 AI 依赖
├── docker-compose.yml                 # RAG 依赖服务编排
├── evaluation/                        # Golden Set、检索和生成评估
├── src/
│   ├── main.py                        # FastAPI 应用与路由
│   ├── config.py                      # 运行、安全和检索配置
│   ├── api/                           # 诊断、搜索、同步、反馈和安全接口
│   ├── rag_pipeline/                  # 召回、重排、Embedding 和内容治理
│   ├── storage/                       # Redis、Pub/Sub 和幂等存储
│   └── utils/                         # 指标、质量评估和租户上下文
└── tests/                             # RAG、安全、检索和同步测试

app/
├── start_app_v2.sh                    # 前后端启动脚本，生产环境禁用 reload
├── backend/
│   ├── routers/
│   │   ├── auth.py                    # 认证、登出和 OIDC 路由
│   │   └── console.py                 # 配置中心及控制台接口
│   ├── services/
│   │   ├── console_agent.py           # 三类 Agent 编排和 LLM 调用
│   │   ├── console_ai.py              # 单轮诊断和 AI 能力
│   │   ├── console_common.py          # 配置默认值、校验、权限和脱敏
│   │   ├── console_kb.py              # 知识库治理、去重扫描和合并建议
│   │   ├── llm_runtime.py             # 全局及 Agent 作用域 LLM 解析
│   │   └── ...                         # 知识、同步、质量和健康度服务
│   ├── tests/
│   │   ├── test_agent_quality.py      # Agent 质量与预算测试
│   │   ├── test_agent_repeat_consistency.py # 诊断一致性测试
│   │   ├── test_kb_dedup_similarity.py # 知识去重相似度阈值测试
│   │   └── test_llm_agent_scope.py    # Agent 独立 LLM 配置回归测试
│   └── alembic/                       # 数据库迁移
└── frontend/
    ├── src/lib/console-api.ts         # 控制台类型及配置、去重 API
    ├── src/pages/console/KbPage.tsx   # 知识库去重合并页面
    ├── src/pages/console/OpsPage.tsx  # 运维配置中心和 Agent 配置分组
    ├── src/pages/HelpPage.tsx         # 帮助和功能说明
    └── src/pages/                     # Dashboard、Agent、知识和运维页面
```

`.atoms/`、`.mgx/`、测试缓存、日志、临时脚本和构建产物不属于源码发布内容。

# File Description Inventory

| 文件 | 说明 |
|---|---|
| `app/backend/services/console_kb.py` | 实现知识库去重扫描、相似度分组和合并建议，使用 80% 模板相似度阈值并返回组内相似度指标。 |
| `app/backend/services/llm_runtime.py` | 按 Agent 作用域解析 Provider、地址、密钥、模型、温度和超时，并逐项继承全局配置。 |
| `app/backend/services/console_common.py` | 定义配置默认值、校验规则、密钥集合、脱敏和配置中心辅助逻辑。 |
| `app/backend/services/console_agent.py` | 编排诊断、知识治理和值班 Agent 的 LLM 调用，并记录生效配置快照。 |
| `app/backend/services/console_ai.py` | 处理单轮诊断及其独立诊断配置继承。 |
| `app/backend/routers/console.py` | 提供配置读写、知识治理和按 Agent 作用域的 LLM 连通性测试接口。 |
| `app/backend/tests/test_kb_dedup_similarity.py` | 验证 80% 边界成组和低相似度同集群不成组。 |
| `app/backend/tests/test_llm_agent_scope.py` | 验证三类 Agent 的独立配置、字段继承、密钥处理、校验和透传。 |
| `app/frontend/src/lib/console-api.ts` | 定义全局及 Agent LLM 配置、去重扫描类型和请求响应类型。 |
| `app/frontend/src/pages/console/KbPage.tsx` | 展示去重合并建议、组内最低相似度及候选案例相似度徽标。 |
| `app/frontend/src/pages/console/OpsPage.tsx` | 展示全局默认与三个 Agent 独立配置分组，支持保存、继承、脱敏和测试。 |
| `app/frontend/src/pages/HelpPage.tsx` | 展示知识库去重合并规则和控制台功能帮助。 |
| `app/start_app_v2.sh` | 启动应用并在生产环境阻止 `uvicorn --reload`。 |
| `docs/CONSOLE_USER_GUIDE.md` | 说明配置中心分组、逐项继承、密钥脱敏、连通性测试和去重阈值。 |
| `docs/KB_GOVERNANCE_AGENT_DESIGN.md` | 说明知识治理 Agent 的独立 LLM 配置及 80% 去重合并规则。 |
| `docs/DIAGNOSE_AGENT_DESIGN.md` | 说明诊断 Agent 的独立 LLM 参数、预算和快照。 |
| `docs/ONCALL_AGENT_DESIGN.md` | 说明值班 Agent 的独立 LLM 配置。 |
| `docs/OPERATIONS_DEPLOYMENT_GUIDE.md` | 说明配置下发、动态生效及生产启动保护。 |

# Technology Stack

- **后端**：Python、FastAPI、Uvicorn、asyncio
- **AI 编排**：LangChain、LCEL、ReAct、结构化输出
- **LLM 运行时**：全局默认配置、Agent 作用域覆盖、动态配置下发、Fernet 密钥加密
- **向量检索**：Milvus、BGE-M3 Embedding
- **业务重排**：规则过滤、多特征融合、BGE Cross-Encoder、LLM Listwise
- **消息与缓存**：Kafka、Redis、Redis Pub/Sub、epoch 语义缓存
- **数据存储**：PostgreSQL、Alembic、SQLite 测试存储
- **可靠性**：补偿队列、指数退避、死信、人工重放、幂等、版本守卫和进程守护
- **安全治理**：API Key/JWT、RBAC、Fernet、租户隔离、内容安全和审计
- **质量评估**：Golden Set、Golden Corpus、Agent 质量评估、Trust Index、一致性测试
- **前端与交付**：TypeScript、React、Vite、pnpm、Docker Compose、Nginx、systemd/supervisor
- **质量保障**：pytest、pytest-asyncio、ESLint、GitHub Actions、compileall

# Usage

## 安装依赖

```bash
bash install.sh
```

按模块跳过安装：

```bash
bash install.sh --skip-rag
bash install.sh --skip-backend
bash install.sh --skip-frontend
```

单独安装依赖：

```bash
cd aiops-rag-system
pip install -r requirements.txt

cd ../app/frontend
pnpm install
```

## 配置

根据模板创建本地配置：

- `aiops-rag-system/.env.example`
- `app/backend/.env.example`
- `app/frontend/.env.example`

配置数据库、Milvus、Kafka、Redis、Embedding、LLM、CMDB、认证密钥和租户环境。

知识库去重阈值默认配置为：

```bash
KB_DEDUP_SIMILARITY_THRESHOLD=0.8
```

只有同错误类型、同服务且模板相似度达到 80% 的案例才会展示合并建议；同集群不再作为低相似度案例的兜底成组条件。扫描结果会附带组内两两相似度和最低相似度。

全局 LLM 配置作为默认值；三个 Agent 使用以下作用域：

- `diagnose`
- `kb_governance`
- `oncall`

每个作用域支持 Provider、Base URL、API Key、Model、Temperature 和 Timeout。Agent 配置字段为空时逐项继承全局 `llm_*` 配置，不要求整组覆盖。API Key 使用加密存储并脱敏回显。配置保存后动态生效，无需重启。

诊断时间预算仍可配置：

```bash
DIAGNOSE_TIME_BUDGET_SECONDS=90
```

预算允许范围为 30–600 秒，覆盖 ReAct、强制收尾和降级调用。

生产环境配置：

```bash
ENVIRONMENT=production
ENABLE_DEMO_LOGIN=false
```

生产环境启动脚本会自动禁用 `--reload`，应配合 systemd 或 supervisor 等进程守护方案。

## 数据库迁移

```bash
cd app/backend
alembic upgrade head
```

## 测试与检查

运行后端专项测试：

```bash
cd app/backend
pytest tests/test_kb_dedup_similarity.py
pytest tests/test_llm_agent_scope.py
pytest tests/test_agent_quality.py
pytest tests/test_agent_repeat_consistency.py
python -m compileall .
```

运行 RAG 测试：

```bash
cd aiops-rag-system
pytest
python -m compileall src
```

运行前端检查：

```bash
cd app/frontend
pnpm run lint
pnpm run build
```

本轮验证结果：

- 后端回归 24 项全部通过，包含 2 项去重阈值专项测试。
- 前端 ESLint 和 Vite build 通过。
- 去重扫描验证 80% 边界可成组，低相似度同集群案例不成组。
- 配置中心完整下发 15 个 Agent 接入键，密钥标记正确。
- 全局继承、独立覆盖、密钥脱敏、动态生效和连通性测试通过。

## 运行服务

运行 RAG 服务：

```bash
cd aiops-rag-system
uvicorn src.main:app
```

控制台后端按照 `app/backend/README.md` 准备数据库、依赖和环境变量后启动。

前端开发模式：

```bash
cd app/frontend
pnpm run dev
```

生产环境不得使用 `--reload`，应由进程守护工具负责自动拉起。

## 知识库去重合并

1. 进入知识库治理页面。
2. 打开“去重合并”功能并执行相似案例扫描。
3. 检查系统仅展示同错误类型、同服务且相似度不低于 80% 的案例组。
4. 查看组内最低相似度及候选案例相似度徽标。
5. 根据案例内容和相似度人工确认是否合并。
6. 对相似度低于 80% 的同集群案例，不应显示合并建议。

## 配置中心验证

1. 进入运维页面的配置中心。
2. 检查全局 LLM 默认配置及三个 Agent 配置分组。
3. 为任一 Agent 单独修改模型、温度或超时，确认其他字段仍继承全局值。
4. 验证 API Key 保存后仅以脱敏占位符显示。
5. 使用每组的连通性测试，确认返回实际生效配置及来源。
6. 保存配置后直接执行诊断、知识治理和值班任务，确认无需重启即可生效。
7. 清空 Agent 字段，确认恢复逐项继承全局配置。
8. 检查配置写入审计日志及配置下发结果。

## 可信度与质量评估

```bash
cd aiops-rag-system
python -m evaluation.retrieval_eval
python -m evaluation.generation_eval
```

质量重点关注：

- Faithfulness、Context Coverage、Answer Relevance、Citation、幻觉率和 Trust Index。
- Precision@K、Recall@K、MRR 和 Hit Rate。
- Agent 输入、候选、模型参数、工具轨迹和持久化快照。
- LLM 实际 Provider、模型、温度、超时及配置来源。
- 跨租户召回、内容安全、知识新鲜度和版本完整性。
- 生产后端进程是否由 systemd/supervisor 守护，是否错误启用 `--reload`。
