# Project Summary

项目是面向企业知识运营、智能运维和知识治理的统一 AIOps 平台。平台接收外部告警，经事件标准化、消息队列、缓存、向量检索、业务重排和 Agent 推理，输出根因诊断、值班报告、ChatOps 文本及知识治理结果，并通过 Web 控制台管理知识库、案例、规则、会话、审批和运维任务。

当前业务闭环包括：

- **告警诊断**：通过 ReAct Agent、多层检索重排和证据链校验完成根因分析，并展示召回、质量、Embedding、重排、Token 和端到端耗时指标。
- **诊断稳定性**：诊断默认使用 `diagnose_temperature=0`，固定评估上下文、候选集合、规则版本、模型参数和稳定排序，支持成功及降级路径重复运行一致性验证。
- **诊断超时防护**：支持 `diagnose_time_budget_seconds` 墙钟时间预算，默认 90 秒，可配置范围为 30–600 秒；预算耗尽时跳过后续 LLM 降级调用并返回结构化结果。
- **值班报告**：按服务、负责人和严重级别聚合影响面，生成持久化值班报告、ChatOps 文本和降级报告。
- **知识库构建与同步**：支持案例 upsert、delete、verify、版本守卫、补偿同步、缓存失效和反馈回流。
- **知识治理**：支持告警聚类、案例草稿、审批发布、相似案例合并、归档、内容安全扫描及生命周期巡检。
- **质量与可信度治理**：统一输出 Faithfulness、Context Coverage、Answer Relevance、幻觉率、Trust Index、未支持断言和达标判断。
- **安全隔离**：RAG 路由支持 API Key/JWT 鉴权，并按租户、集群和环境隔离检索范围。
- **可靠性治理**：补偿任务支持并发领取锁、指数退避、死信、人工重放和审计。
- **认证容错**：登出接口同时兼容 GET/POST；OIDC 登出地址构建失败时采用 fail-open，避免异常升级为 500。

本轮完成后端进程空窗、诊断代理超时和登出异常的加固。生产部署继续要求使用 systemd/supervisor 等进程守护，避免使用 `--reload` 造成源站不可达。后端 Agent 专项测试 15 项、编译检查、前端 lint/build、健康检查及配置中心验证全部通过。

# Project Module Description

- **RAG 告警流水线**
  - 接收并标准化告警，生成事件指纹。
  - 通过 Kafka、Redis、Milvus 和 LLM 完成召回、重排和诊断。
  - 支持请求认证、租户/集群/环境过滤和降级处理。
  - 输出证据质量、引用、召回、耗时和 Token usage 指标。

- **知识搜索服务**
  - 提供只读知识案例搜索接口。
  - 使用向量召回和 L1-L4 业务重排。
  - 执行 API Key/JWT 鉴权及数据隔离。
  - RAG 失败时回退 PostgreSQL 限量扫描，默认上限为 200。
  - 返回召回数量、重排统计、Embedding 耗时和端到端耗时。

- **知识发布与同步**
  - 支持案例 upsert、delete、verify 和反馈同步。
  - 失败任务进入补偿队列，支持指数退避、死信、人工重放和审计。
  - 使用 `FOR UPDATE SKIP LOCKED` 避免任务重复领取。
  - 5xx、网络异常和缓存失效失败会触发补偿。
  - 409 `stale_version_rejected` 视为最终状态，不重复入队。

- **版本守卫与幂等**
  - Milvus 持久化 `kb_version`。
  - 同内容旧版本请求幂等放行。
  - 同版本内容变化可重新索引。
  - 内容变化且版本低于当前索引版本时拒绝覆盖。
  - 兼容缺少版本字段的旧集合。

- **语义缓存**
  - 使用 epoch 标识缓存版本。
  - 发布、删除和反馈变更时执行缓存失效。
  - 通过 Redis Pub/Sub 广播多实例失效事件。

- **四层业务重排**
  - L1：按有效期、黑名单、反馈和废弃状态硬过滤。
  - L2：融合语义、拓扑、时间衰减、反馈、模板、新鲜度和命中率。
  - L3：使用 BGE Cross-Encoder 精排。
  - L4：使用 LLM Listwise 排序，失败时回退至 L3/L2。
  - 各级融合排序使用稳定 tie-breaker，避免等分候选顺序漂移。

- **检索安全与内容治理**
  - 支持统一 API Key/JWT 请求认证。
  - 缺少或错误 Key 返回 401，缺少访问范围返回 403。
  - 支持 `X-API-Key` 与 Bearer 认证方式。
  - 按租户、集群和环境过滤检索范围，阻断跨租户召回。
  - 对知识写入内容执行密钥、PII、危险命令和提示词注入检测。
  - 支持摘要脱敏、跨字段去重、误报放行和 `allow_override`。

- **深度诊断 Agent**
  - 支持多轮 ReAct、最多 3 个工具并行调用、格式纠错、证据链校验和失败降级。
  - 支持 `diagnose_temperature` 配置，默认值为 0，并透传至每次 LLM 调用。
  - 支持 `diagnose_time_budget_seconds`，默认 90 秒，范围为 30–600 秒。
  - ReAct 推理、强制收尾和降级链路共享墙钟预算；预算耗尽时停止推理并跳过 LLM 降级。
  - 预算耗尽返回结构化 `diagnose_time_budget_exhausted` 错误，避免请求超过边缘代理超时。
  - 固定诊断输入指纹、知识候选 local/merged ID 序列、规则版本和模型参数快照。
  - 固定工具轨迹、候选排序、质量评估上下文及结果归一化逻辑。
  - 持久化事件输入、模型参数、工具轨迹、质量、稳定性和诊断版本。
  - 成功路径和降级路径均支持同一输入重复运行一致性验证。
  - `confidence` 为 Agent 自评把握度；Trust Index 基于 Faithfulness、Context Coverage 和幻觉率计算。

- **值班 Agent**
  - 按 CMDB 服务、负责人和严重级别聚合影响面。
  - 生成 AI 值班报告、ChatOps 文本和确定性降级报告。
  - 以 CMDB 影响面和告警窗口作为 grounding，生成报告质量评估。
  - 质量结果写入 `oncall_reports.report_json.quality` 和会话 `result_json.quality`。
  - 缺少告警窗口时不评估并返回 `quality=null`，评估异常静默降级。

- **知识治理 Agent**
  - 支持告警聚类、案例草稿、审批发布、相似案例扫描、合并、归档和生命周期巡检。
  - 每条草稿记录 Faithfulness、Context Coverage、Answer Relevance、幻觉率、Trust Index、未支持断言和达标判断。
  - 聚合输出平均指标、达标率和样本数。
  - 固化 `cluster_events → ai_draft → submit_change_sets → draft_quality → merge_proposal` 工具轨迹。
  - 质量结果写入 `agent_sessions.result_json.quality`，审计结果携带 `trust_index`。

- **Agent 质量展示**
  - Agents 页面为诊断、知识治理和值班报告展示统一质量卡。
  - 展示 Trust Index、四项明细指标、质量线、未支持断言和达标状态。
  - 展示诊断确定性徽标、模型参数快照、稳定性信息和时间预算。
  - 知识治理同时展示单条草稿指标和聚合质量卡。
  - 值班报告支持查看当前及历史报告质量。
  - 空质量结果不渲染空卡。

- **反馈闭环**
  - 诊断反馈通过控制台与 RAG 双写。
  - 更新案例质量和检索评分，并进行幂等去重。
  - 反馈变更触发缓存失效和质量统计更新。

- **权限与认证**
  - 控制台支持 OIDC、Token Exchange、演示登录、Bearer JWT 和 RBAC。
  - RAG 关键路由统一支持 API Key 鉴权。
  - `/webhook` 保持既有免鉴权契约。
  - 登出接口兼容 GET/POST；OIDC URL 构建失败时 fail-open 返回空跳转地址。
  - 未绑定角色的账号默认使用 `sre`。
  - `operator` 只能触发诊断和值班报告。

- **知识健康度与可信度评估**
  - 聚合案例活跃、归档、未验证、待同步、死信和老化状态。
  - 提供 Golden Set、Golden Corpus、Precision@K、Recall@K、MRR 和 Hit Rate。
  - 支持 Faithfulness、Citation、幻觉率和 Trust Index 等生成质量指标。
  - 内容安全、引用追溯、版本完整性和知识健康度共同支撑可信度治理。

# Directory Tree

```text
README.md                              # 项目总览、模块边界与安全说明
INSTALL.md                             # 安装与部署说明
LICENSE                                # Apache-2.0 许可证
CONTRIBUTING.md                        # 贡献与 PR 规范
CODE_OF_CONDUCT.md                     # 社区行为准则
SECURITY.md                            # 漏洞报告与安全要求
install.sh                             # 一键安装脚本
.github/workflows/ci.yml               # 持续集成配置

docs/
├── DEMO_ACCOUNTS.md                   # 演示账号说明
├── CONSOLE_USER_GUIDE.md              # 控制台使用手册、质量卡和稳定性说明
├── CONSOLE_FAQ.md                     # 常见问题
├── OPERATIONS_DEPLOYMENT_GUIDE.md     # 部署、鉴权、租户、环境和诊断配置
├── OPERATIONS_RUNBOOK.md              # 值班、质量告警和故障处理手册
├── KB_GOVERNANCE_AGENT_DESIGN.md      # 知识治理 Agent、草稿质量和持久化设计
├── DIAGNOSE_AGENT_DESIGN.md           # 深度诊断 Agent、预算控制和一致性设计
├── ONCALL_AGENT_DESIGN.md             # 值班 Agent、报告质量和持久化设计
├── RAG_TRUST_EVALUATION.md            # RAG 可信度、质量和老化评估报告
└── OPEN_SOURCE_RELEASE_CHECKLIST.md   # 开源发布检查清单

aiops-rag-system/
├── README.md                          # RAG 与业务重排说明
├── requirements.txt                   # Python 与 AI 依赖
├── docker-compose.yml                 # RAG 依赖服务编排
├── .env.example                       # RAG、鉴权、租户和环境配置模板
├── evaluation/
│   ├── golden_corpus.jsonl            # Golden Corpus 评估语料
│   ├── golden_set_v1.jsonl            # Golden Set v1 标注数据
│   ├── retrieval_eval.py              # 检索质量评估
│   ├── generation_eval.py             # 生成质量评估
│   ├── metrics.py                     # 离线评估指标计算
│   └── __init__.py                    # 评估模块初始化
├── src/
│   ├── main.py                        # FastAPI 应用、路由和缓存失效订阅
│   ├── config.py                      # 运行、缓存、安全、租户和重排配置
│   ├── kafka_producer.py              # Kafka 消息生产
│   ├── models/                        # 案例、事件和响应模型
│   ├── api/
│   │   ├── diagnostic.py              # 诊断接口
│   │   ├── feedback.py                # 反馈写入与幂等处理
│   │   ├── kb_cache.py                # 缓存失效接口
│   │   ├── kb_search.py               # 鉴权隔离的知识搜索接口
│   │   ├── kb_sync.py                 # 同步、版本守卫和内容校验接口
│   │   └── security.py                # 请求认证与安全依赖
│   ├── rag_pipeline/
│   │   ├── rerank_pipeline.py         # 检索、融合和稳定排序编排
│   │   ├── reranker_l2.py             # L2 业务特征融合重排
│   │   ├── reranker_l3.py             # L3 Cross-Encoder 精排
│   │   └── ...                        # 内容安全、Embedding、Milvus 和流水线
│   ├── storage/redis_client.py        # Redis、Pub/Sub 和反馈幂等标记
│   └── utils/                         # 指标、质量评估和租户上下文
└── tests/
    ├── test_api.py                    # API、认证和隔离行为测试
    ├── test_golden_eval.py            # Golden Set 评估测试
    ├── test_diagnosis_quality.py      # 单次诊断质量指标测试
    ├── test_kb_search.py              # 知识搜索接口测试
    ├── test_kb_sync_idempotency.py    # 同步幂等和版本守卫测试
    └── test_security_auth_tenant_poison.py # 鉴权、隔离和投毒专项测试

app/
├── backend/
│   ├── README.md                      # 后端说明
│   ├── .env.example                   # 后端配置模板
│   ├── scripts/                       # 演示数据、验证和修复脚本
│   ├── alembic/                       # 数据库迁移
│   ├── models/                        # 数据模型
│   ├── routers/
│   │   ├── auth.py                    # 认证、登出和 OIDC 路由
│   │   └── ...                        # 控制台与 Dashboard 路由
│   ├── services/
│   │   ├── console_agent.py           # Agent 编排、诊断预算和结果持久化
│   │   └── console_common.py          # 配置、权限、上下文、审计和脱敏
│   └── tests/
│       ├── test_agent_quality.py      # Agent 质量和预算专项测试
│       └── test_agent_repeat_consistency.py # 重复运行一致性 E2E 测试
└── frontend/
    ├── README.md                      # 前端说明
    ├── .env.example                   # 前端配置模板
    ├── pnpm-lock.yaml                 # 依赖锁定文件
    └── src/
        ├── components/console/shared.tsx # 质量与内容安全共享组件
        ├── lib/console-api.ts         # 控制台 API、会话和质量类型
        └── pages/                     # Dashboard、Agent、事件、知识和运维页面
```

`.atoms/`、`.mgx/`、测试缓存、安装包、日志、临时脚本和构建产物不属于源码发布内容。

# File Description Inventory

| 文件 | 说明 |
|---|---|
| `aiops-rag-system/src/rag_pipeline/rerank_pipeline.py` | 编排多级检索融合，并保证候选排序稳定。 |
| `aiops-rag-system/src/rag_pipeline/reranker_l2.py` | 执行业务特征融合和确定性排序。 |
| `aiops-rag-system/src/rag_pipeline/reranker_l3.py` | 执行 Cross-Encoder 精排及稳定 tie-breaker。 |
| `aiops-rag-system/src/api/security.py` | 提供 RAG 请求认证、安全依赖和访问控制。 |
| `aiops-rag-system/src/utils/quality_eval.py` | 计算诊断质量、引用、忠实度、幻觉和 Trust Index。 |
| `aiops-rag-system/src/api/kb_search.py` | 提供带认证、隔离过滤和检索统计的知识搜索接口。 |
| `aiops-rag-system/src/api/kb_sync.py` | 提供同步、删除、校验、幂等、版本冲突和内容检查。 |
| `aiops-rag-system/src/rag_pipeline/pipeline.py` | 编排召回、重排、同步、版本守卫、质量采集和缓存失效。 |
| `aiops-rag-system/evaluation/retrieval_eval.py` | 基于 Golden Set 评估检索质量。 |
| `aiops-rag-system/evaluation/generation_eval.py` | 评估诊断和报告生成质量。 |
| `aiops-rag-system/tests/test_security_auth_tenant_poison.py` | 验证鉴权、身份解析、租户隔离、表达式防护和投毒规则。 |
| `app/backend/routers/auth.py` | 提供认证与登出路由，兼容 GET/POST 并对 OIDC URL 构建失败执行 fail-open。 |
| `app/backend/services/console_agent.py` | 编排 Agent 生成流程，执行诊断墙钟预算、超时降级并持久化稳定性快照。 |
| `app/backend/services/console_common.py` | 提供配置读取、权限判断、租户上下文、审计和脱敏。 |
| `app/backend/services/quality_scan.py` | 聚合单次质量、内容安全和 Dashboard 质量统计。 |
| `app/backend/services/console_ai.py` | 处理 AI 诊断、生成和质量评估。 |
| `app/backend/services/console_kb.py` | 处理知识治理、审批、发布和内容安全扫描。 |
| `app/backend/services/kb_health.py` | 生成案例状态、同步任务和老化分级报告。 |
| `app/backend/services/rag_sync.py` | 执行控制台到 RAG 的同步、补偿、并发领取和版本处理。 |
| `app/backend/tests/test_agent_quality.py` | 覆盖 Agent 质量、预算解析、预算耗尽降级和达标判定。 |
| `app/backend/tests/test_agent_repeat_consistency.py` | 使用脚本化 LLM 桩和 SQLite 内存库验证成功及降级诊断的一致性。 |
| `app/frontend/src/components/console/shared.tsx` | 提供质量指标卡和内容安全扫描卡。 |
| `app/frontend/src/lib/console-api.ts` | 定义 Dashboard、诊断、Agent 会话、质量及稳定性指标类型。 |
| `app/frontend/src/pages/console/AgentsPage.tsx` | 展示诊断、知识治理和值班会话的质量、稳定性和历史报告指标。 |
| `app/frontend/src/pages/console/OpsPage.tsx` | 展示知识健康报告、质量趋势和诊断时间预算配置。 |
| `app/frontend/src/pages/Index.tsx` | 展示 Trust Index、质量统计和知识健康摘要。 |
| `docs/DIAGNOSE_AGENT_DESIGN.md` | 说明诊断温度、时间预算、上下文快照、Trust Index 和一致性口径。 |
| `docs/CONSOLE_USER_GUIDE.md` | 说明控制台质量卡、稳定性指标、预算配置和指标解释。 |
| `docs/OPERATIONS_DEPLOYMENT_GUIDE.md` | 说明部署、鉴权、租户环境、进程守护和诊断预算配置。 |
| `docs/ONCALL_AGENT_DESIGN.md` | 说明值班报告 grounding、质量字段和持久化链路。 |
| `docs/KB_GOVERNANCE_AGENT_DESIGN.md` | 说明草稿质量、聚合指标、工具轨迹和审计字段。 |
| `docs/RAG_TRUST_EVALUATION.md` | 记录质量、可信度、老化和安全评估口径。 |

# Technology Stack

- **后端**：Python、FastAPI、Uvicorn、asyncio
- **AI 编排**：LangChain、LCEL、ReAct、结构化输出
- **向量检索**：Milvus、BGE-M3 Embedding
- **业务重排**：规则过滤、多特征融合、BGE Cross-Encoder、LLM Listwise
- **消息与缓存**：Kafka、Redis、Redis Pub/Sub、epoch 语义缓存
- **数据存储**：PostgreSQL、Alembic、SQLite 测试存储
- **可靠性**：补偿队列、指数退避、死信、人工重放、幂等、版本守卫、数据库并发锁和进程守护
- **安全治理**：API Key/JWT、RBAC、租户/集群/环境隔离、内容安全检查、审计
- **质量评估**：Golden Set、Golden Corpus、在线诊断评估、Agent 质量评估、Trust Index、重复运行一致性测试
- **可观测性**：Prometheus、任务审计、工具轨迹、模型参数快照、Token usage、检索和质量指标
- **前端与交付**：TypeScript、Vite、pnpm、Docker Compose、Nginx、systemd/supervisor
- **质量保障**：pytest、pytest-asyncio、JUnit XML、compileall、ESLint、GitHub Actions

# Usage

## 安装依赖

```bash
bash install.sh
```

按模块跳过安装：

```bash
bash install.sh --skip-rag
bash install.sh --skip-backend
bash install.sh --skip-frontend
```

单独安装依赖：

```bash
cd aiops-rag-system
pip install -r requirements.txt

cd ../app/frontend
pnpm install
```

## 配置

根据模板创建本地配置：

- `aiops-rag-system/.env.example`
- `app/backend/.env.example`
- `app/frontend/.env.example`

配置数据库、Milvus、Kafka、Redis、Embedding、LLM、CMDB、认证密钥、租户/集群环境和重排参数。

RAG 鉴权配置示例：

```bash
RAG_AUTH_ENABLED=true
RAG_API_KEYS_JSON='{"example-key":{"tenant":"default","cluster":"default","environment":"default"}}'
RAG_DEFAULT_TENANT=default
```

生产环境应设置强随机 `JWT_SECRET_KEY` 和 `MASK_KEY`，并关闭演示登录：

```bash
ENABLE_DEMO_LOGIN=false
```

诊断确定性配置：

```bash
DIAGNOSE_TEMPERATURE=0
```

诊断时间预算配置：

```bash
DIAGNOSE_TIME_BUDGET_SECONDS=90
```

预算默认值为 90 秒，允许范围为 30–600 秒。配置中心可实时暴露并更新该配置。预算覆盖多轮 ReAct、强制收尾和降级调用；预算耗尽时返回结构化 `diagnose_time_budget_exhausted` 结果。

知识健康度可配置：

```bash
KB_EXPIRE_UNCONDITIONAL_DAYS=180
```

未配置时回退为 `2 × KB_EXPIRE_DAYS`。原始日志、模型输入、诊断报告和 ChatOps 文本应经过脱敏处理。

## 数据库迁移

```bash
cd app/backend
alembic upgrade head
```

若数据库由种子脚本创建且没有 Alembic 版本记录，应先标记到对应基线，再执行最新迁移。

## 测试与检查

运行 RAG 全量测试：

```bash
cd aiops-rag-system
pytest
python -m compileall src
```

运行安全专项测试：

```bash
pytest tests/test_security_auth_tenant_poison.py
```

运行质量和 Golden Set 测试：

```bash
pytest tests/test_diagnosis_quality.py
pytest tests/test_golden_eval.py
```

运行后端 Agent 测试：

```bash
cd ../app/backend
pytest tests/test_agent_quality.py
pytest tests/test_agent_repeat_consistency.py
python -m compileall .
```

运行前端检查：

```bash
cd ../frontend
pnpm run lint
pnpm run build
```

本轮验证结果：

- 后端 Agent 专项测试：15 passed。
- 重复诊断一致性、预算解析和预算耗尽降级测试通过。
- 后端 `py_compile` 和全量回归通过。
- 前端 lint/build 通过。
- 运行实例健康检查通过。
- 配置中心已暴露 `diagnose_time_budget_seconds=90`。
- 真实诊断完成时间为 24.9 秒，持久化 `time_budget_seconds=90.0`。

## 初始化和数据修复

使用演示数据时执行种子脚本。数据库曾被显式主键写入污染时，可执行：

```bash
cd app/backend
python scripts/fix_sequences.py
```

执行前需确保数据库连接配置有效，并同步写入健康度配置。

## 运行服务

运行 RAG 服务：

```bash
cd aiops-rag-system
uvicorn src.main:app
```

控制台后端按照 `app/backend/README.md` 准备数据库、依赖和环境变量后启动。

生产环境不得使用 `--reload`。应使用 systemd 或 supervisor 等进程守护方案，确保后端异常退出后自动拉起并减少源站空窗导致的代理 5xx。

前端开发模式：

```bash
cd app/frontend
pnpm run dev
```

## 知识治理与同步验证

1. 创建或更新案例并提交审批。
2. 验证内容安全检查、租户归属和请求认证。
3. 审批发布后验证 RAG upsert 和 `kb_version`。
4. 重复提交同内容旧版本，确认幂等放行。
5. 提交低版本内容变更，确认返回 409 且不会覆盖新知识。
6. 模拟 RAG 5xx、网络异常或缓存失效失败，确认进入补偿队列。
7. 并发执行补偿任务，确认不会重复领取。
8. 验证退避重试、死信和人工重放。
9. 执行 delete、回滚、合并和归档，验证索引及缓存失效。
10. 使用 verify 按 `case_id` 检查索引可见性和统计字段。
11. 执行生命周期巡检 dry-run。
12. 在 KbPage 和 ApprovalsPage 验证安全扫描、误报放行和审批结果。
13. 在 AgentsPage 验证治理草稿、值班报告、诊断质量、稳定性和时间预算卡。
14. 对同一事件恢复相同初始快照后重复执行诊断，验证成功和降级路径输出一致。
15. 验证诊断预算耗尽时跳过 LLM 降级并返回结构化错误。
16. 验证登出 GET/POST 均可正常返回，OIDC URL 构建异常时不返回 500。
17. 在 Dashboard 和 Ops 页面验证质量统计、Trust Index 和知识健康报告。

端到端验证：

```bash
cd app/backend
python scripts/verify_rag_compensation_e2e.py
```

## 权限与隔离验证

- 未绑定角色的账号默认解析为 `sre`。
- `operator` 只能使用诊断和值班报告功能。
- `sre` 及更高等级角色可使用知识治理和同步运维功能。
- 使用 API Key/JWT 验证未认证请求的 401 和无权限请求的 403。
- 验证不同 tenant、cluster 和 environment 只能召回授权范围内知识。
- 验证 upsert、verify、delete 和 kb-search 的租户边界。
- 验证 `/webhook` 继续保持免鉴权契约。
- 验证登出 GET/POST 方法兼容及 OIDC fail-open 行为。
- 验证治理、值班和诊断会话的质量、模型参数快照、稳定性和预算字段。
- 不应将 `default_role` 设置为 `sys_admin`。

## 可信度与质量评估

```bash
cd aiops-rag-system
python -m evaluation.retrieval_eval
python -m evaluation.generation_eval
```

质量口径：

- `confidence` 是 Agent 对本次结论的主观自评，默认阈值为 `0.75`。
- Trust Index 是基于证据一致性的客观评分：

  `T = 0.4 × Faithfulness + 0.35 × Context Coverage + 0.25 × (1 − 幻觉率)`

- 单次质量线为 `0.85`，用于达标判断和人工复核提示。
- 知识治理和值班报告复用诊断质量评估口径，但使用各自的 grounding 数据。
- 值班报告缺少告警窗口时返回 `quality=null`，不渲染空质量卡。
- 评估异常执行静默降级，不影响报告和会话主结果持久化。
- 同一事件的一致性验证必须使用相同初始事件、候选、规则和配置快照。
- 诊断预算耗尽属于可观测的结构化降级状态，不应继续发起新的 LLM 调用。

重点关注：

- Faithfulness、Context Coverage、Answer Relevance、Citation、幻觉率、Trust Index 和端到端耗时。
- Precision@K、Recall@K、MRR 和 Hit Rate。
- 输入、候选、排序、工具轨迹、模型参数、输出和持久化字段是否一致。
- Dashboard 的生成、检索、内容安全、Agent 质量和知识健康聚合。
- 跨租户召回是否为零。
- 投毒规则的拦截、脱敏和误报放行结果。
- 知识新鲜度、版本完整性、验证状态和反馈质量变化。
- Faithfulness 低于 0.85 时按 Runbook 执行质量告警和治理流程。
- 后端进程是否持续存活，生产是否由 systemd/supervisor 守护，是否仍使用 `--reload`。
