# Project Summary

项目提供面向企业知识运营、智能运维与知识治理的统一 AIOps 平台。平台接收外部告警，经过事件标准化、消息队列、缓存、向量检索和业务重排，生成根因诊断、值班报告与知识治理结果，并通过 Web 控制台管理知识库、案例、规则、Agent 会话、审批和运维治理。

平台包含三类 Agent：

- **深度诊断 Agent**：使用 ReAct 多轮推理，结合告警、CMDB、日志样本、规则和知识库进行根因分析。
- **值班 Agent**：聚合服务影响面、负责人和严重级别，生成值班报告及 ChatOps 文本。
- **知识治理 Agent**：执行告警聚类、案例草稿、相似案例治理、审批发布和生命周期管理。

本次更新重点完成深度诊断 Agent 的生产化优化：知识检索改为 RAG 优先并支持受限降级，证据链与工具轨迹交叉校验，支持格式纠错预算、并行工具调用、诊断版本保留、失败会话事务兜底、人工反馈回流、置信度归一化和 Token usage 持久化。

RAG 流水线基于 LangChain 和 Milvus，使用多层重排输出参考案例。新增只读知识搜索接口，支持召回与四层重排，不依赖 LLM，异常时采用 fail-open 策略。

# Project Module Description

- **RAG 告警流水线**：接收并标准化告警，生成事件指纹，通过 Kafka、Redis、Milvus 和 LLM 完成诊断。
- **知识搜索服务**：
  - 提供只读知识案例搜索接口。
  - 通过向量召回和 L1-L4 业务重排返回候选案例。
  - 搜索异常时返回安全降级结果，不阻断诊断流程。
  - 控制台 `search_kb` 优先调用 RAG，失败时使用 PostgreSQL 限量扫描降级，默认扫描上限为 200。
- **四层业务重排**：
  - L1：按有效期、黑名单、反馈和废弃状态硬过滤。
  - L2：融合语义、拓扑、时间衰减、反馈、模板、新鲜度和命中率。
  - L3：使用 BGE Cross-Encoder 精排。
  - L4：使用 LLM Listwise 排序，失败时回退至 L3/L2。
- **深度诊断 Agent**：
  - 支持多轮 ReAct 推理和最多 3 个工具并行调用。
  - 非法 JSON 使用独立的两次格式纠错预算，不占用推理轮次。
  - 证据链必须能够匹配工具观察结果，减少模型虚构证据。
  - 支持 Agent 失败后单轮诊断降级，并记录失败会话。
  - 重跑前将旧结论标记为 `superseded`，保留诊断版本历史。
  - 统一归一化 0~1、0~10 和 0~100 三种 confidence 表示。
  - 记录工具轨迹、结果截断信息和 Token usage。
- **值班 Agent**：按 CMDB 服务、负责人和严重级别聚合影响面，生成 AI 报告和确定性降级报告。
- **知识治理 Agent**：支持告警聚类、案例草稿、幂等保护、相似案例扫描、审批发布、会话恢复、归档和生命周期巡检。
- **反馈闭环**：
  - 支持诊断结果人工反馈。
  - 反馈通过控制台与 RAG 双写，更新案例质量和检索评分。
- **Agent 工作台**：
  - 诊断和值班操作需要 `can_diagnose`。
  - 知识治理操作需要 `can_edit_kb`。
  - 未绑定角色的账号默认使用 `sre`。
  - `operator` 只能触发诊断和值班报告。
  - `sys_admin` 不可作为默认角色。
- **权限与认证**：支持 OIDC、Token Exchange、演示登录、Bearer JWT 和 RBAC，后端执行最终授权。
- **知识库与案例**：管理知识条目、案例版本、审批记录、拓扑、变更记录和 RAG 索引状态。
- **审批中心**：处理案例变更、规则升级及新建、更新、合并和归档审批。
- **规则管理**：支持规则校验、发布、回滚、晋升、废弃和未知告警治理。
- **生命周期治理**：支持案例归档和定期巡检，按有效期或长期未更新条件发现候选案例。
- **性能与监控**：记录 RAG 阶段、重排层、Agent 延迟、降级次数、Token usage 和知识同步统计。

# Directory Tree

```text
README.md                              # 项目总览、模块边界与安全说明
INSTALL.md                             # 安装与部署说明
LICENSE                                # Apache-2.0 许可证
CONTRIBUTING.md                        # 贡献与 PR 规范
CODE_OF_CONDUCT.md                     # 社区行为准则
SECURITY.md                            # 漏洞报告与安全要求
install.sh                             # 一键安装脚本
.github/workflows/ci.yml               # 持续集成配置

docs/
├── DEMO_ACCOUNTS.md                   # 演示账号说明
├── CONSOLE_USER_GUIDE.md              # 控制台使用手册
├── CONSOLE_FAQ.md                     # 常见问题
├── OPERATIONS_DEPLOYMENT_GUIDE.md     # 部署方案
├── OPERATIONS_RUNBOOK.md              # 值班与故障处理手册
├── KB_GOVERNANCE_AGENT_DESIGN.md      # 知识治理 Agent 设计
├── DIAGNOSE_AGENT_DESIGN.md           # 深度诊断 Agent 设计
├── ONCALL_AGENT_DESIGN.md             # 值班 Agent 设计
└── OPEN_SOURCE_RELEASE_CHECKLIST.md   # 开源发布检查清单

aiops-rag-system/
├── README.md                          # RAG 与业务重排说明
├── requirements.txt                   # Python 与 AI 依赖
├── docker-compose.yml                 # RAG 依赖服务编排
├── .env.example                       # RAG 配置模板
├── src/
│   ├── main.py                        # FastAPI 应用、探针与路由注册
│   ├── config.py                      # 运行与重排配置
│   ├── kafka_producer.py              # Kafka 消息生产
│   ├── models/                        # 案例和响应模型
│   ├── api/
│   │   ├── diagnostic.py              # 诊断接口
│   │   ├── kb_sync.py                 # 知识索引同步接口
│   │   └── kb_search.py               # 只读知识搜索接口
│   ├── rag_pipeline/
│   │   ├── milvus_client.py           # Milvus 检索、索引操作与案例查询
│   │   ├── retriever.py               # LangChain 检索封装
│   │   └── pipeline.py                # 召回、重排和结果统计编排
│   ├── storage/redis_client.py        # Redis 访问
│   └── utils/metrics.py               # Prometheus 指标
└── tests/
    ├── test_kb_search.py              # 知识搜索接口测试
    └── ...                            # RAG、API、同步和性能测试

app/
├── backend/
│   ├── README.md                      # 后端说明
│   ├── .env.example                   # 后端配置模板
│   ├── scripts/                       # 演示数据和数据库修复脚本
│   ├── alembic/                       # 数据库迁移
│   ├── routers/
│   │   ├── console.py                 # 控制台业务、归档与生命周期路由
│   │   └── console_agent.py           # Agent、会话、反馈和报告路由
│   └── services/
│       ├── console_common.py          # 公共配置、权限、角色默认值与脱敏工具
│       ├── console_agent.py           # Agent 编排、证据校验与会话持久化
│       ├── console_kb.py              # 知识库、案例治理和反馈处理
│       └── rag_sync.py                # 控制台到 RAG 的同步客户端
└── frontend/
    ├── README.md                      # 前端说明
    ├── .env.example                   # 前端配置模板
    ├── pnpm-lock.yaml                 # 依赖锁定文件
    └── src/
        ├── lib/console-api.ts         # API 类型与调用封装
        ├── components/                # 通用组件与布局
        └── pages/                     # 控制台和帮助页面
```

`.atoms/`、`.mgx/`、测试缓存、安装包、日志、临时脚本和构建产物不属于源码发布内容。内部模板文档不应纳入开源发布。

# File Description Inventory

| 文件 | 说明 |
|---|---|
| `aiops-rag-system/src/main.py` | 注册诊断、知识同步和知识搜索接口。 |
| `aiops-rag-system/src/api/kb_search.py` | 提供只读知识搜索和 RAG 结果返回接口。 |
| `aiops-rag-system/src/api/kb_sync.py` | 提供案例 upsert、delete、verify 同步 API。 |
| `aiops-rag-system/src/rag_pipeline/pipeline.py` | 执行案例召回、四层重排、统计和 fail-open 降级。 |
| `aiops-rag-system/src/rag_pipeline/retriever.py` | 将 Milvus 检索封装为 LangChain `BaseRetriever`。 |
| `aiops-rag-system/src/rag_pipeline/milvus_client.py` | 管理 Milvus 索引、案例查询和统计字段。 |
| `aiops-rag-system/tests/test_kb_search.py` | 覆盖知识搜索接口及降级行为。 |
| `app/backend/routers/console.py` | 提供控制台业务、案例归档和生命周期巡检接口。 |
| `app/backend/services/console_agent.py` | 实现 ReAct 编排、并行工具调用、证据校验、反馈、版本保留、失败会话和 usage 持久化。 |
| `app/backend/services/console_kb.py` | 实现知识库治理、审批、发布、回滚、合并、归档和反馈回流。 |
| `app/backend/services/rag_sync.py` | 封装控制台调用 RAG 搜索、同步和反馈接口的客户端。 |
| `app/backend/services/console_common.py` | 定义配置、权限、角色默认值、输入处理和敏感信息脱敏。 |
| `app/frontend/src/lib/console-api.ts` | 定义控制台 API、权限、Agent 和反馈调用类型。 |
| `app/frontend/src/pages/console/AgentsPage.tsx` | 展示 Agent 任务、会话、历史报告和诊断结果。 |
| `app/frontend/src/pages/console/EventsPage.tsx` | 管理告警详情、诊断结果和人工反馈。 |
| `app/frontend/src/pages/console/KbPage.tsx` | 管理案例发布、回滚、归档和相似案例合并。 |
| `app/frontend/src/pages/console/ApprovalsPage.tsx` | 展示审批证据、字段 Diff 和决策操作。 |
| `app/frontend/src/pages/console/RulesPage.tsx` | 管理规则版本和未知告警队列。 |
| `app/frontend/src/pages/console/OpsPage.tsx` | 提供审计、系统配置和治理运维页面。 |

# Technology Stack

- **后端**：Python、FastAPI、Uvicorn、asyncio
- **AI 编排**：LangChain、LCEL、ReAct、结构化输出
- **向量检索**：Milvus、BGE-M3 Embedding
- **业务重排**：规则过滤、多特征融合、BGE Cross-Encoder、LLM Listwise
- **消息与缓存**：Kafka、Redis
- **数据存储**：PostgreSQL、Alembic
- **可观测性**：Prometheus、Agent 降级指标、工具轨迹、Token usage 和审计日志
- **认证授权**：OIDC、JWT、RBAC
- **前端与交付**：TypeScript、Vite、pnpm、Docker Compose、Nginx
- **质量保障**：pytest、JUnit XML、compileall、ESLint、GitHub Actions
- **开源治理**：Apache-2.0、贡献指南、安全策略和发布检查清单

# Usage

## 安装依赖

在源码目录执行：

```bash
bash install.sh
```

按模块跳过安装：

```bash
bash install.sh --skip-rag
bash install.sh --skip-backend
bash install.sh --skip-frontend
```

单独安装依赖：

```bash
cd aiops-rag-system
pip install -r requirements.txt

cd ../app/frontend
pnpm install
```

## 配置

根据模板创建本地配置：

- `aiops-rag-system/.env.example`
- `app/backend/.env.example`
- `app/frontend/.env.example`

除数据库、Milvus、Kafka、Redis、Embedding、LLM、CMDB 和重排参数外，还需配置控制台访问 RAG 服务的地址及超时参数。

生产环境必须设置强随机 `JWT_SECRET_KEY` 和 `MASK_KEY`，并关闭演示登录：

```bash
ENABLE_DEMO_LOGIN=false
```

原始日志、模型输入、诊断报告和 ChatOps 文本应经过敏感信息脱敏处理。

## 测试与检查

```bash
cd aiops-rag-system
pytest
python -m compileall src

cd ../app/backend
python -m compileall .

cd ../frontend
pnpm run lint
pnpm run build
```

当前 RAG 全量测试结果为 67 passed、0 failed、0 errors，其中包含知识搜索新增测试。后端静态编译和控制台导入检查通过。

## 初始化和数据修复

使用演示数据时执行种子脚本。数据库曾被显式主键写入污染时，可执行：

```bash
cd app/backend
python scripts/fix_sequences.py
```

执行前需确保数据库连接配置有效。

## 运行服务

运行 RAG 服务：

```bash
cd aiops-rag-system
uvicorn src.main:app
```

控制台后端按照 `app/backend/README.md` 准备数据库、依赖和环境变量后启动。前端开发模式使用：

```bash
cd app/frontend
pnpm run dev
```

## 知识治理与诊断验证

1. 创建或更新案例并提交审批。
2. 审批发布后验证 RAG upsert 结果。
3. 执行案例回滚、合并和归档，验证对应索引操作。
4. 使用知识搜索接口验证召回、重排和异常降级。
5. 执行诊断，确认工具轨迹、证据链和 Token usage 已保存。
6. 重跑同一事件，确认旧结论保留为 `superseded`。
7. 模拟 RAG、工具或 Agent 异常，确认降级结果和 `failed` 会话落库。
8. 提交诊断反馈，确认反馈同步至案例评分和 RAG。
9. 使用 verify 按 `case_id` 检查索引可见性和统计字段。
10. 执行生命周期巡检 dry-run，确认到期或长期未更新候选。

## 权限验证

- 未绑定角色的账号默认解析为 `sre`，可访问三个 Agent 操作。
- `operator` 只能使用诊断和值班报告功能。
- `sre` 及更高等级角色可使用知识治理功能。
- 账号角色可在审计与配置中通过 `role_bindings_json` 调整。
- 不应将 `default_role` 设置为 `sys_admin`。

## CI 验证

GitHub Actions 自动执行：

1. RAG pytest；
2. 后端 Python 静态编译检查；
3. 前端 ESLint 和生产构建。
