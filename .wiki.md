# Project Summary

项目是面向企业知识运营、智能运维与知识治理的统一 AIOps 平台。平台接收外部告警，经事件标准化、消息队列、缓存、向量检索和业务重排后，生成根因诊断、值班报告与知识治理结果，并通过 Web 控制台管理知识库、案例、规则、Agent 会话、审批和运维治理。

平台包含：

- **深度诊断 Agent**：基于 ReAct 多轮推理，结合告警、CMDB、日志、规则和知识库进行根因分析。
- **值班 Agent**：聚合服务影响面、负责人和严重级别，生成值班报告及 ChatOps 文本。
- **知识治理 Agent**：执行告警聚类、案例草稿、相似案例治理、审批发布和生命周期管理。

RAG 流水线基于 LangChain 和 Milvus，采用向量召回与四层业务重排输出参考案例。知识发布支持补偿队列、重试退避、死信、人工重放、缓存失效广播和业务降级。当前新增知识版本守卫：通过 `kb_version` 防止旧版本同步或补偿任务覆盖新知识，并在控制台侧正确处理过期版本冲突。

# Project Module Description

- **RAG 告警流水线**：接收并标准化告警，生成事件指纹，通过 Kafka、Redis、Milvus 和 LLM 完成诊断。
- **知识搜索服务**：
  - 提供只读知识案例搜索接口。
  - 通过向量召回和 L1-L4 业务重排返回候选案例。
  - 异常时采用 fail-open 降级，不阻断诊断流程。
  - 控制台优先调用 RAG，失败时使用 PostgreSQL 限量扫描，默认上限为 200。
- **知识发布与同步**：
  - 支持案例 upsert、delete、verify 和反馈同步。
  - 控制台同步失败时写入补偿任务。
  - 支持指数退避重试、死信、人工重放和任务审计。
  - 5xx、网络异常和缓存失效失败会进入补偿队列。
  - 409 `stale_version_rejected` 视为最终状态，不重复入队。
- **版本守卫与幂等**：
  - Milvus 持久化 `kb_version`。
  - 同内容旧版本请求直接幂等放行。
  - 同版本内容变化正常重新索引。
  - 内容变化且版本低于索引版本时拒绝覆盖。
  - 旧补偿任务遇到 409 后标记完成，并记录 `resolved=stale_version`。
  - 旧集合缺少版本字段时动态兼容，避免强制重建存量数据。
- **语义缓存**：
  - 使用 epoch 标识缓存版本。
  - 发布、删除和反馈变更时执行真实缓存失效。
  - 通过 Redis Pub/Sub 向多实例广播失效事件。
- **四层业务重排**：
  - L1：按有效期、黑名单、反馈和废弃状态硬过滤。
  - L2：融合语义、拓扑、时间衰减、反馈、模板、新鲜度和命中率。
  - L3：使用 BGE Cross-Encoder 精排。
  - L4：使用 LLM Listwise 排序，失败时回退至 L3/L2。
- **深度诊断 Agent**：
  - 支持多轮 ReAct 和最多 3 个工具并行调用。
  - 非法 JSON 使用独立格式纠错预算。
  - 校验证据链与工具观察结果，减少虚构证据。
  - 支持失败降级、失败会话记录、诊断版本保留和人工反馈回流。
  - 统一归一化不同范围的 confidence。
  - 持久化工具轨迹、结果截断信息和 Token usage。
- **值班 Agent**：按 CMDB 服务、负责人和严重级别聚合影响面，生成 AI 报告和确定性降级报告。
- **知识治理 Agent**：支持告警聚类、案例草稿、审批发布、相似案例扫描、归档和生命周期巡检。
- **反馈闭环**：诊断反馈通过控制台与 RAG 双写，更新案例质量和检索评分，并进行幂等处理。
- **权限与认证**：支持 OIDC、Token Exchange、演示登录、Bearer JWT 和 RBAC，后端执行最终授权。
- **Agent 工作台**：
  - 诊断和值班操作需要 `can_diagnose`。
  - 知识治理操作需要 `can_edit_kb`。
  - 未绑定角色的账号默认使用 `sre`。
  - `operator` 只能触发诊断和值班报告。
  - `sys_admin` 不作为默认角色。
- **性能与监控**：记录 RAG 阶段、重排层、Agent 延迟、降级次数、Token usage、同步任务状态和审计日志。

# Directory Tree

```text
README.md                              # 项目总览、模块边界与安全说明
INSTALL.md                             # 安装与部署说明
LICENSE                                # Apache-2.0 许可证
CONTRIBUTING.md                        # 贡献与 PR 规范
CODE_OF_CONDUCT.md                     # 社区行为准则
SECURITY.md                            # 漏洞报告与安全要求
install.sh                             # 一键安装脚本
.github/workflows/ci.yml               # 持续集成配置

docs/
├── DEMO_ACCOUNTS.md                   # 演示账号说明
├── CONSOLE_USER_GUIDE.md              # 控制台使用手册
├── CONSOLE_FAQ.md                     # 常见问题
├── OPERATIONS_DEPLOYMENT_GUIDE.md     # 部署方案
├── OPERATIONS_RUNBOOK.md              # 值班与故障处理手册
├── KB_GOVERNANCE_AGENT_DESIGN.md      # 知识治理 Agent 设计
├── DIAGNOSE_AGENT_DESIGN.md           # 深度诊断 Agent 设计
├── ONCALL_AGENT_DESIGN.md             # 值班 Agent 设计
└── OPEN_SOURCE_RELEASE_CHECKLIST.md   # 开源发布检查清单

aiops-rag-system/
├── README.md                          # RAG 与业务重排说明
├── requirements.txt                   # Python 与 AI 依赖
├── docker-compose.yml                 # RAG 依赖服务编排
├── .env.example                       # RAG 配置模板
├── src/
│   ├── main.py                        # FastAPI 应用、路由和缓存失效订阅
│   ├── config.py                      # 运行、缓存和重排配置
│   ├── kafka_producer.py              # Kafka 消息生产
│   ├── models/
│   │   └── case.py                    # 案例、版本和响应模型
│   ├── api/
│   │   ├── diagnostic.py              # 诊断接口
│   │   ├── feedback.py                # 反馈写入与幂等处理
│   │   ├── kb_sync.py                 # 知识同步、版本守卫和校验接口
│   │   └── kb_cache.py                # 缓存失效接口
│   ├── rag_pipeline/
│   │   ├── embedder.py                # Embedding 与语义缓存
│   │   ├── milvus_client.py           # Milvus 检索、索引和版本字段管理
│   │   ├── retriever.py               # LangChain 检索封装
│   │   └── pipeline.py                # 召回、重排、同步和缓存失效编排
│   ├── storage/redis_client.py        # Redis、Pub/Sub 和反馈幂等标记
│   └── utils/metrics.py               # Prometheus 指标
└── tests/
    ├── test_kb_search.py              # 知识搜索接口测试
    └── test_kb_sync_idempotency.py    # 同步幂等和版本守卫测试

app/
├── backend/
│   ├── README.md                      # 后端说明
│   ├── .env.example                   # 后端配置模板
│   ├── scripts/
│   │   ├── verify_rag_compensation_e2e.py # 补偿闭环端到端验证
│   │   └── ...                        # 演示数据和数据库修复脚本
│   ├── alembic/
│   │   ├── versions/
│   │   │   └── c9d5e2f7a8b1_add_rag_sync_tasks.py # 补偿任务表迁移
│   │   └── ...                        # 数据库迁移
│   ├── models/
│   │   └── rag_sync_tasks.py          # RAG 补偿任务模型
│   ├── routers/
│   │   ├── console.py                 # 控制台业务、归档与生命周期路由
│   │   ├── console_agent.py           # Agent、会话、反馈和报告路由
│   │   └── console_sync_admin.py      # 补偿任务运维管理 API
│   └── services/
│       ├── console_common.py          # 配置、权限、角色和脱敏工具
│       ├── console_agent.py           # Agent 编排和会话持久化
│       ├── console_kb.py              # 知识库治理与发布
│       └── rag_sync.py                # 同步客户端、补偿队列和版本处理
└── frontend/
    ├── README.md                      # 前端说明
    ├── .env.example                   # 前端配置模板
    ├── pnpm-lock.yaml                 # 依赖锁定文件
    └── src/
        ├── lib/console-api.ts         # API 类型与调用封装
        ├── components/                # 通用组件与布局
        └── pages/                     # 控制台和帮助页面
```

`.atoms/`、`.mgx/`、测试缓存、安装包、日志、临时脚本和构建产物不属于源码发布内容。内部模板文档不应纳入开源发布。

# File Description Inventory

| 文件 | 说明 |
|---|---|
| `aiops-rag-system/src/api/kb_sync.py` | 提供案例同步、删除、校验、幂等处理和版本冲突响应。 |
| `aiops-rag-system/src/api/kb_cache.py` | 提供语义缓存失效及广播相关接口。 |
| `aiops-rag-system/src/api/feedback.py` | 处理反馈写入、幂等去重和缓存失效。 |
| `aiops-rag-system/src/models/case.py` | 定义案例载荷、Milvus 映射和 `kb_version` 字段。 |
| `aiops-rag-system/src/rag_pipeline/embedder.py` | 管理 Embedding 及 epoch 语义缓存。 |
| `aiops-rag-system/src/rag_pipeline/milvus_client.py` | 管理 Milvus Schema、案例查询、反馈更新和版本比较。 |
| `aiops-rag-system/src/rag_pipeline/pipeline.py` | 执行召回、重排、同步、版本守卫和缓存失效。 |
| `aiops-rag-system/src/storage/redis_client.py` | 提供 Redis 访问、Pub/Sub 广播和反馈幂等标记。 |
| `aiops-rag-system/src/main.py` | 注册 RAG 路由、探针和缓存失效订阅。 |
| `aiops-rag-system/tests/test_kb_sync_idempotency.py` | 覆盖旧版本拒绝、同版本幂等和同步兼容行为。 |
| `app/backend/models/rag_sync_tasks.py` | 定义补偿任务、重试、死信和审计字段。 |
| `app/backend/alembic/versions/c9d5e2f7a8b1_add_rag_sync_tasks.py` | 创建 RAG 补偿任务表。 |
| `app/backend/services/rag_sync.py` | 执行控制台到 RAG 的同步、补偿入队、重试和 409 清账。 |
| `app/backend/services/console_common.py` | 提供配置读取、权限判断、审计和敏感信息脱敏。 |
| `app/backend/routers/console_sync_admin.py` | 提供补偿任务查询、重试、死信和人工重放运维接口。 |
| `app/backend/scripts/verify_rag_compensation_e2e.py` | 使用真实 PostgreSQL 与 HTTP 桩服务验证补偿闭环。 |
| `app/frontend/src/lib/console-api.ts` | 定义控制台同步任务、权限、Agent 和反馈调用类型。 |
| `app/frontend/src/pages/console/AgentsPage.tsx` | 展示 Agent 任务、会话、历史报告和诊断结果。 |
| `app/frontend/src/pages/console/EventsPage.tsx` | 管理告警详情、诊断结果和人工反馈。 |
| `app/frontend/src/pages/console/KbPage.tsx` | 管理案例发布、回滚、归档和相似案例合并。 |
| `app/frontend/src/pages/console/ApprovalsPage.tsx` | 展示审批证据、字段 Diff 和决策操作。 |
| `app/frontend/src/pages/console/RulesPage.tsx` | 管理规则版本和未知告警队列。 |
| `app/frontend/src/pages/console/OpsPage.tsx` | 提供审计、系统配置和治理运维页面。 |

# Technology Stack

- **后端**：Python、FastAPI、Uvicorn、asyncio
- **AI 编排**：LangChain、LCEL、ReAct、结构化输出
- **向量检索**：Milvus、BGE-M3 Embedding
- **业务重排**：规则过滤、多特征融合、BGE Cross-Encoder、LLM Listwise
- **消息与缓存**：Kafka、Redis、Redis Pub/Sub、epoch 语义缓存
- **数据存储**：PostgreSQL、Alembic
- **可靠性**：补偿队列、指数退避、死信、人工重放、幂等和版本守卫
- **可观测性**：Prometheus、任务审计、工具轨迹、Token usage 和同步指标
- **认证授权**：OIDC、JWT、RBAC
- **前端与交付**：TypeScript、Vite、pnpm、Docker Compose、Nginx
- **质量保障**：pytest、JUnit XML、compileall、ESLint、GitHub Actions
- **开源治理**：Apache-2.0、贡献指南、安全策略和发布检查清单

# Usage

## 安装依赖

在源码目录执行：

```bash
bash install.sh
```

按模块跳过安装：

```bash
bash install.sh --skip-rag
bash install.sh --skip-backend
bash install.sh --skip-frontend
```

单独安装依赖：

```bash
cd aiops-rag-system
pip install -r requirements.txt

cd ../app/frontend
pnpm install
```

## 配置

根据模板创建本地配置：

- `aiops-rag-system/.env.example`
- `app/backend/.env.example`
- `app/frontend/.env.example`

除数据库、Milvus、Kafka、Redis、Embedding、LLM、CMDB 和重排参数外，还需配置：

- 控制台访问 RAG 的地址和超时。
- 补偿队列重试、退避、死信和批量处理参数。
- 语义缓存 epoch、Pub/Sub 和失效策略。

生产环境必须设置强随机 `JWT_SECRET_KEY` 和 `MASK_KEY`，并关闭演示登录：

```bash
ENABLE_DEMO_LOGIN=false
```

原始日志、模型输入、诊断报告和 ChatOps 文本应经过敏感信息脱敏处理。

## 数据库迁移

首次使用或升级时执行：

```bash
cd app/backend
alembic upgrade head
```

若数据库由种子脚本创建且没有 Alembic 版本记录，应先将已有迁移链标记到对应基线，再执行最新迁移，避免重复创建既有表。

## 测试与检查

运行 RAG 全量测试：

```bash
cd aiops-rag-system
pytest
python -m compileall src
```

运行后端检查：

```bash
cd ../app/backend
python -m compileall .
```

运行前端检查：

```bash
cd ../frontend
pnpm run lint
pnpm run build
```

当前验证结果：

- RAG：78 passed、0 failed、0 skipped。
- 补偿闭环 E2E：37/37 通过。
- 后端改动文件 `py_compile` 全部通过。

## 初始化和数据修复

使用演示数据时执行种子脚本。数据库曾被显式主键写入污染时，可执行：

```bash
cd app/backend
python scripts/fix_sequences.py
```

执行前需确保数据库连接配置有效。

## 运行服务

运行 RAG 服务：

```bash
cd aiops-rag-system
uvicorn src.main:app
```

控制台后端按照 `app/backend/README.md` 准备数据库、依赖和环境变量后启动。前端开发模式使用：

```bash
cd app/frontend
pnpm run dev
```

## 知识治理与同步验证

1. 创建或更新案例并提交审批。
2. 审批发布后验证 RAG upsert 和 `kb_version`。
3. 重复提交同内容旧版本，确认幂等放行。
4. 提交低版本内容变更，确认返回 409 且不会覆盖新知识。
5. 模拟 RAG 5xx、网络异常或缓存失效失败，确认任务进入补偿队列。
6. 验证退避重试、死信和人工重放。
7. 让旧补偿任务遇到 409，确认任务完成清账且记录版本冲突原因。
8. 执行 delete、回滚、合并和归档，验证索引及缓存失效。
9. 使用 verify 按 `case_id` 检查索引可见性和统计字段。
10. 执行生命周期巡检 dry-run，确认到期或长期未更新候选。

端到端验证脚本：

```bash
cd app/backend
python scripts/verify_rag_compensation_e2e.py
```

该脚本使用真实 PostgreSQL 和 HTTP 桩 RAG 服务，覆盖成功同步、失败入队、幂等、重试、死信、人工重放、反馈去重、删除补偿、缓存失效、业务降级和版本守卫。

## 权限验证

- 未绑定角色的账号默认解析为 `sre`，可访问三个 Agent 操作。
- `operator` 只能使用诊断和值班报告功能。
- `sre` 及更高等级角色可使用知识治理和同步运维功能。
- 账号角色可在审计与配置中通过 `role_bindings_json` 调整。
- 不应将 `default_role` 设置为 `sys_admin`。

## CI 验证

GitHub Actions 自动执行：

1. RAG pytest；
2. 后端 Python 静态编译检查；
3. 前端 ESLint 和生产构建。
