# Project Summary

项目提供面向企业知识运营、智能运维与知识治理的统一 AIOps 平台。平台接收外部告警，经事件标准化、消息队列、缓存、向量检索和多层业务重排生成根因诊断，并通过 Web 控制台维护知识库、案例、规则、Agent 流程及审批治理。

当前 RAG 流水线基于 LangChain 重构：Milvus 检索封装为 `BaseRetriever`，业务重排封装为文档压缩器，并通过 LCEL 编排提示词、LLM 与结构化输出。候选案例经过 Top-20 召回、L1 规则过滤、L2 多特征融合、L3 Cross-Encoder 精排和可选的 L4 LLM Listwise 重排，最终输出 Top-3 参考案例。

平台支持逐层降级：LLM 失败回退至 L3，Cross-Encoder 不可用回退至 L2，全部高级能力失效时保留 L1 结果。重排指标、降级计数和最终候选数量通过 Prometheus 暴露，反馈数据可用于案例评分和后续模型训练。

知识治理 Agent 已完成故障修复和设计文档沉淀。问题根因为演示数据种子脚本显式写入主键后未同步 PostgreSQL 序列，导致创建审批单时发生主键冲突。现已在种子脚本完成后自动重置业务表序列，并通过 24 小时和 7 天窗口验证知识治理链路恢复正常。

# Project Module Description

- **RAG 告警流水线**：接收 Webhook，标准化告警、生成事件指纹、写入 Kafka/Redis，并通过 Milvus 和 LLM 完成诊断。
- **LangChain 检索层**：
  - 将 Milvus 查询结果转换为 LangChain `Document`。
  - 通过 `BaseRetriever` 统一检索接口。
  - 通过 LCEL 连接提示词模板、ChatOpenAI 和 JSON/Pydantic 结构化输出。
- **四层业务重排**：
  - **L1 规则硬过滤**：剔除过期、黑名单、低反馈或包含废弃方案的案例。
  - **L2 多特征融合**：综合语义、拓扑、时间衰减、反馈、模板重叠、新鲜度和命中率，输出 Top-10。
  - **L3 Cross-Encoder 精排**：使用 BGE Reranker 输出 Top-5，并与 L2 分数融合。
  - **L4 LLM Listwise 重排**：根据根因、拓扑、时效、反馈和方案可执行性排序，输出 Top-3；失败时自动降级。
- **知识治理 Agent**：
  - 按 24 小时或 7 天时间窗口聚类告警。
  - 对模板相似事件执行幂等保护和相似案例扫描。
  - 生成案例草稿、变更集和合并提案。
  - 支持审批、发布、会话恢复、审计持久化及 AI 失败降级。
  - 申请人不可审批自身提交的变更。
- **性能治理**：
  - 通过 `rag_stage_latency_seconds` 和 `rerank_layer_latency_seconds` 监控阶段延迟。
  - Embedding 支持缓存、熔断和本地模型预热。
  - Milvus 支持集合句柄缓存、查询超时和可配置检索参数。
  - LLM 支持超时、重试抑制、熔断和相似案例降级。
  - L4 在独立线程中执行，避免阻塞主 RAG 链路。
- **反馈闭环**：记录 `upvotes`、`downvotes`、`hit_count` 和 `recall_count`，兼容保留 `feedback_score`。
- **健康检查与监控**：提供健康、就绪和 Prometheus 指标接口，监控重排延迟、降级次数和最终结果数量。
- **知识库与案例**：管理知识条目、案例、版本、实例日志、拓扑信息和变更记录。
- **去重合并**：扫描相似案例、管理合并提案、确认或撤销合并并记录审计。
- **审批中心**：处理案例变更、规则升级、新建和更新案例审批，展示证据及字段 Diff。
- **Agent 工作台**：支持深度诊断、知识治理、会话恢复、CMDB 查询和历史值班报告。
- **规则管理**：支持规则查看、校验、发布、回滚、晋升、废弃及未知告警队列治理。
- **认证与权限**：支持 OIDC、Token Exchange、演示登录、登出和 Bearer JWT，后端执行最终授权。
- **用户与角色**：管理用户、角色及访问范围，角色层级为 `viewer < operator < sre < approver < kb_admin < sys_admin`。
- **审计与配置**：系统管理员维护审计日志、系统配置和 LLM 连通性。
- **帮助中心**：提供用户手册、FAQ、部署手册和值班应急手册。

# Directory Tree

```text
README.md                              # 项目总览、模块边界、开源与安全说明
INSTALL.md                             # 安装与部署说明
LICENSE                                # Apache-2.0 许可证
CONTRIBUTING.md                        # 贡献、测试与 PR 规范
CODE_OF_CONDUCT.md                     # 社区行为准则
SECURITY.md                            # 漏洞报告与安全部署要求
install.sh                             # 一键安装脚本
.github/
└── workflows/
    └── ci.yml                         # GitHub Actions 持续集成
docs/
├── DEMO_ACCOUNTS.md                   # 演示账号说明
├── CONSOLE_USER_GUIDE.md              # 控制台使用手册
├── CONSOLE_FAQ.md                     # 控制台常见问题
├── OPERATIONS_DEPLOYMENT_GUIDE.md     # 部署方案
├── OPERATIONS_RUNBOOK.md              # 值班与故障处理手册
├── KB_GOVERNANCE_AGENT_DESIGN.md      # 知识治理 Agent 设计说明书
└── OPEN_SOURCE_RELEASE_CHECKLIST.md   # 开源发布审计清单

aiops-rag-system/
├── README.md                          # RAG 与四层业务重排说明
├── requirements.txt                   # Python 与 LangChain 依赖
├── docker-compose.yml                 # RAG 依赖服务编排
├── .env.example                       # RAG、重排和性能参数模板
├── .gitignore                         # RAG 子项目忽略规则
├── src/
│   ├── main.py                        # FastAPI 应用、业务接口和探针
│   ├── config.py                      # 运行、重排和降级配置
│   ├── kafka_producer.py              # Kafka 消息生产
│   ├── models/
│   │   ├── case.py                    # 案例及反馈数据模型
│   │   └── response.py                # 诊断和重排响应模型
│   ├── api/
│   │   └── diagnostic.py              # 诊断接口与非阻塞执行入口
│   ├── rag_pipeline/
│   │   ├── embedder.py                # Embedding、缓存、熔断与预热
│   │   ├── documents.py               # 业务数据与 LangChain Document 转换
│   │   ├── llm_client.py              # LLM 调用、超时、重试与降级
│   │   ├── milvus_client.py           # Milvus 连接、Schema 和检索
│   │   ├── pipeline.py                # RAG 主流程、重排编排与耗时统计
│   │   ├── prompt_builder.py          # ChatPromptTemplate 和诊断提示词
│   │   ├── retriever.py               # LangChain BaseRetriever 封装
│   │   ├── rerank_pipeline.py         # 四层重排、分数融合和降级
│   │   ├── reranker_l1.py             # 规则硬过滤
│   │   ├── reranker_l2.py             # 多特征业务评分
│   │   ├── reranker_l3.py             # BGE Cross-Encoder 精排
│   │   └── reranker_l4.py             # LLM Listwise 重排
│   ├── storage/
│   │   └── redis_client.py            # Redis 事件暂存与状态访问
│   └── utils/
│       └── metrics.py                 # RAG 与重排 Prometheus 指标
└── tests/
    ├── test_api.py                    # RAG API 与探针测试
    ├── test_rag_pipeline.py           # 主流程和兼容性测试
    ├── test_p99_optimizations.py      # P99 优化专项测试
    └── test_business_rerank.py        # 四层重排、融合和降级测试

app/
├── backend/
│   ├── README.md                      # 控制台后端说明
│   ├── .env.example                   # 后端环境变量模板
│   ├── scripts/
│   │   ├── seed_console_demo.py       # 演示数据种子及序列自动重置
│   │   └── fix_sequences.py           # PostgreSQL 业务表序列修复
│   ├── skills_docs/                   # 内部能力模板文档，不应发布
│   ├── alembic/                       # 数据库迁移
│   ├── routers/
│   │   ├── console.py                 # 控制台业务路由
│   │   └── console_agent.py           # Agent、会话和报告路由
│   └── services/
│       ├── console_agent.py           # Agent 执行与结果持久化
│       └── console_kb.py              # 知识库、案例、日志和审批服务
└── frontend/
    ├── README.md                      # 控制台前端说明
    ├── .env.example                   # 前端环境变量模板
    ├── pnpm-lock.yaml                 # 前端依赖锁定文件
    ├── dist/                          # 构建生成的生产产物
    └── src/
        ├── lib/console-api.ts         # API 类型与调用封装
        ├── components/                # 通用组件和控制台布局
        └── pages/                     # 帮助中心及业务页面
```

`.atoms/`、`.mgx/`、测试缓存、安装包、上传图片、日志、临时脚本和其他构建产物不属于源码发布内容。`app/backend/skills_docs/` 等平台内部模板文档不应纳入开源发布。

# File Description Inventory

| 文件 | 说明 |
|---|---|
| `docs/KB_GOVERNANCE_AGENT_DESIGN.md` | 知识治理 Agent 的系统定位、运行链路、API 契约、聚类与幂等、审批发布、降级、权限、审计、监控及复现说明。 |
| `app/backend/scripts/seed_console_demo.py` | 写入演示数据，并在完成后自动重置业务表 ID 序列，避免重复执行造成主键冲突。 |
| `app/backend/scripts/fix_sequences.py` | 修复 11 张业务表的 PostgreSQL 序列，使显式主键写入后的自增 ID 保持同步。 |
| `aiops-rag-system/requirements.txt` | 锁定 RAG、LangChain、模型和服务依赖。 |
| `aiops-rag-system/.env.example` | 提供检索、四层重排、超时、缓存和指标参数。 |
| `aiops-rag-system/src/config.py` | 解析重排层开关、候选数量、权重、超时和模型配置。 |
| `aiops-rag-system/src/models/case.py` | 定义案例、拓扑和反馈统计字段。 |
| `aiops-rag-system/src/models/response.py` | 定义诊断、候选案例和结构化 LLM 输出。 |
| `aiops-rag-system/src/rag_pipeline/documents.py` | 在业务记录与 LangChain `Document` 之间转换。 |
| `aiops-rag-system/src/rag_pipeline/retriever.py` | 将 Milvus 检索封装为 LangChain `BaseRetriever`。 |
| `aiops-rag-system/src/rag_pipeline/rerank_pipeline.py` | 编排 L1-L4 重排、分数融合和逐层回退。 |
| `aiops-rag-system/src/rag_pipeline/reranker_l1.py` | 按有效期、黑名单、反馈和废弃方案进行硬过滤。 |
| `aiops-rag-system/src/rag_pipeline/reranker_l2.py` | 计算语义、拓扑、时间、反馈、模板、新鲜度和命中率特征。 |
| `aiops-rag-system/src/rag_pipeline/reranker_l3.py` | 使用 BGE Cross-Encoder 进行候选精排。 |
| `aiops-rag-system/src/rag_pipeline/reranker_l4.py` | 使用结构化 Prompt 进行 LLM Listwise 排序。 |
| `aiops-rag-system/src/rag_pipeline/pipeline.py` | 串联检索、重排、诊断和分阶段指标。 |
| `aiops-rag-system/src/rag_pipeline/llm_client.py` | 管理 LLM 超时、熔断、重试和降级。 |
| `aiops-rag-system/src/utils/metrics.py` | 定义 RAG 延迟、重排层延迟、降级和最终结果指标。 |
| `aiops-rag-system/tests/test_rag_pipeline.py` | 验证重构后的主流程、模型和依赖兼容性。 |
| `aiops-rag-system/tests/test_business_rerank.py` | 覆盖四层漏斗、分数融合、LLM ID 校验和降级路径。 |
| `aiops-rag-system/tests/test_p99_optimizations.py` | 覆盖缓存、超时、熔断和 P99 优化场景。 |
| `app/backend/routers/console.py` | 提供认证后的事件、知识库、审批、规则、审计和配置路由。 |
| `app/backend/routers/console_agent.py` | 提供 Agent 诊断、知识治理、会话、CMDB 和值班报告路由。 |
| `app/backend/services/console_kb.py` | 实现知识库、案例、治理、审批和合并服务。 |
| `app/frontend/src/lib/console-api.ts` | 定义控制台 API 类型、分页、诊断和权限调用。 |
| `app/frontend/src/pages/console/EventsPage.tsx` | 管理告警详情、AI 诊断和反馈。 |
| `app/frontend/src/pages/console/KbPage.tsx` | 管理案例、关联日志和相似案例合并。 |
| `app/frontend/src/pages/console/AgentsPage.tsx` | 展示 Agent 任务、会话恢复和历史报告。 |
| `app/frontend/src/pages/console/ApprovalsPage.tsx` | 展示审批详情、证据、字段 Diff 和决策操作。 |
| `app/frontend/src/pages/console/RulesPage.tsx` | 展示规则版本和未知告警队列。 |
| `app/frontend/src/pages/console/OpsPage.tsx` | 提供管理员专属审计和配置页面。 |

# Technology Stack

- **后端**：Python、FastAPI、Uvicorn
- **AI 编排**：LangChain、LCEL、ChatPromptTemplate、JsonOutputParser/Pydantic
- **向量检索**：Milvus、BGE-M3 Embedding
- **业务重排**：规则过滤、多特征融合、BGE Cross-Encoder、LLM Listwise
- **消息与缓存**：Kafka、Redis
- **智能能力**：规则引擎、向量检索、LLM、CMDB、Agent 会话持久化
- **数据存储**：PostgreSQL、Alembic
- **可观测性**：Prometheus Histogram、重排延迟、降级和结果计数
- **认证授权**：OIDC、JWT、RBAC
- **交付部署**：Docker Compose、Bash 安装脚本、Nginx 静态托管
- **质量保障**：pytest、Python compileall、ESLint、Vite、GitHub Actions
- **开源治理**：Apache-2.0、贡献指南、行为准则、安全策略和发布检查清单

# Usage

## 安装依赖

在源码目录执行：

```bash
bash install.sh
```

可按模块跳过安装：

```bash
bash install.sh --skip-rag
bash install.sh --skip-backend
bash install.sh --skip-frontend
```

单独安装 RAG 依赖：

```bash
cd aiops-rag-system
pip install -r requirements.txt
```

单独安装前端依赖：

```bash
cd app/frontend
pnpm install
```

## 配置与安全

根据模板创建本地配置：

- `aiops-rag-system/.env.example`
- `app/backend/.env.example`
- `app/frontend/.env.example`

生产环境必须设置随机且强度足够的 `JWT_SECRET_KEY` 和 `MASK_KEY`，并关闭演示登录：

```bash
ENABLE_DEMO_LOGIN=false
```

根据运行环境配置 Milvus、Kafka、Redis、Embedding、LLM、数据库及重排参数。L4 默认可关闭；开启后应设置合理超时，并确认失败时回退至 L3/L2。

## RAG 测试与静态检查

```bash
cd aiops-rag-system
pytest
python -m compileall src
```

## 控制台后端检查

```bash
cd app/backend
python -m compileall .
```

使用演示数据时，执行种子脚本后会自动重置业务表 ID 序列。若数据库曾被显式主键写入污染，可运行：

```bash
python scripts/fix_sequences.py
```

运行前需确保数据库连接配置可用。

## 前端检查与构建

```bash
cd app/frontend
pnpm run lint
pnpm run build
pnpm run dev
```

## RAG 服务运行

在 `aiops-rag-system` 目录准备依赖和环境变量后运行：

```bash
uvicorn src.main:app
```

## 控制台后端运行

按照 `app/backend/README.md` 准备 Python 依赖、数据库和环境变量后启动服务。

## 性能验证

1. 准备可用的 Milvus、Embedding、LLM、Kafka 和 Redis 依赖。
2. 对诊断接口施加递增并发压力。
3. 通过指标接口观察各 RAG 阶段及 L1-L4 重排层的 P99 延迟。
4. 分别关闭 L4、L3 和高级重排能力，验证逐层降级结果。
5. 调整候选数量、模型批次、超时、缓存和融合权重后复测。
6. 通过案例反馈统计评估 NDCG@3、Recall@3、MRR、采纳率和 MTTR。
7. 使用 24 小时和 7 天时间窗口验证知识治理 Agent 的聚类、草稿幂等、合并提案和审批链路。

## CI 验证

GitHub Actions 自动执行：

1. RAG pytest；
2. 控制台后端 Python 静态编译检查；
3. 前端 ESLint 和生产构建。

## 发布前检查

- 审查 Markdown 是否包含平台内部模板、私有路径或内部工具说明。
- 确认示例凭证仅用于演示并明确生产替换要求。
- 扫描真实 JWT、API Key、密码和非示例 IP。
- 移除 `.atoms/`、`.mgx/`、`skills_docs/` 等不应公开的内容。
- 对已被 Git 跟踪的忽略文件执行索引清理。
- 按开源发布清单完成最终一致性验证。
